# 연금 Agent — 프로젝트 컨텍스트

제10회 미래에셋증권 AI Festival · 연금 Agent 트랙 출품작.
자연어 연금 질의에 대해 제공 문서 근거로 조회·분석·설명하는 AI 에이전트.

## 절대 제약 (위반 시 실격 또는 감점)

1. **LLM은 HyperCLOVA X만 사용**. 다른 LLM 모델 사용 시 평가대상에서 제외.
   - 임베딩/NLI 모델이 이 범위에 포함되는지 미확인 → 확인 전까지 검증 계층은 전부 결정론적 코드로 구현
2. **09.06 마감 이후 커밋·push·배포 금지** (실격 사유)
3. **평가는 세션 없는 단일 GET 요청** — 멀티턴 대화 불가
4. **단정적 추천 금지** — 확인조건 제시 후 상황별 결론 제공

## 평가 API 스펙 (변경 불가)

```
GET /answer?question_id={id}&question={질의}

200 OK
{
  "question_id": "Q-001",
  "question": "평가 질의 원문",
  "retrieved_context": "답변 생성에 참고한 검색 문서",
  "think_trace": "사고·추론·도구 사용 과정",
  "answer": "최종 생성 답변"
}
```

5개 필드 전부 필수. `retrieved_context`가 비는 경우에도 "근거 문서 없음"을 명시할 것.

## 핵심 설계 원칙 (구현 시 절대 위반 금지)

| 원칙 | 의미 |
|---|---|
| 계산은 함수, 설명은 LLM | LLM이 숫자를 생성하지 않는다. 결정론적 함수 출력만 설명한다 |
| 판단은 코드, 문장은 LLM | 답변가능성·제약 위반 판정을 LLM 재량에 맡기지 않는다 |
| 사용한 것만 인용 | 검색된 근거 전부가 아니라 답변을 뒷받침한 근거만 제시한다 |
| 모르면 되묻는다 | 조건 미확인 시 단정하지 않되, 확인 항목은 최대 2건 |
| 감사는 심각도를 올릴 수만 있다 | LLM 감사가 결정론적 판정을 완화하지 못한다 (단조성) |

## 아키텍처 — L0부터 L6까지

```
L0  사전 검색      grounding_retrieval.py · LLM 없음
                   도메인 커버리지 판정 · 문서 용어 수집 · 구법 사전경고
                   → 도메인 밖이면 조기 REFUSE (LLM 호출 0회로 종결)
L1  질의 분석      HyperCLOVA X · Function calling (L0 접지 정보 주입)
                   → 요구사항 슬롯 + 호출할 계산함수 선택 + 실행 계획
1.5 계획 감사      supervisory_board.supervise_plan() · LLM 없음
                   화이트리스트 검증 · 슬롯-계획 정합 · 미등록 호출 제거
L2  함정 감지      trap_rules.py · 26종 규칙 (결정론적)
L3  Exploration    pgvector + BM25 정밀 검색
L4  Exploitation   구법탐지 → 엔티티충돌 → 가입자격 → 하드제약 (순차)
L5  Prediction     pension_calc_functions.py · CALC_REGISTRY 15종 (LLM 미개입)
L5' Supervisor     HyperCLOVA X · 조건부 설명 생성 (숫자 생성 금지)
L6  감독 이사회    supervisory_board.py
                   결정론적 4대 감사(준법/이상치/적합성/부담)
                   + HyperCLOVA X 의미 감사(정합성/태도/의도충족/오해유발)
                   → APPROVE / REVISE / DOWNGRADE / BLOCK
                   REVISE 시 시정 지시와 함께 L5'로 1회 되돌림
```

**L0 결과는 절대 답변 근거로 쓰지 않는다.** L0는 Exploitation 필터를 거치지 않았으므로,
인용하거나 `retrieved_context`에 넣거나 계산 파라미터로 공급하면 안 된다.
L1에 넘기는 것도 원문이 아니라 `as_analysis_hint()`가 반환하는 영역·용어 목록뿐이다.
원문을 통째로 주면 L1이 이를 근거로 착각해 답변을 만들어낼 위험이 있다.

**LLM 호출은 3개소다 — L1, L5', L6.** L0와 계획 감사는 LLM을 쓰지 않으므로
이 계층들을 추가해도 호출 횟수는 늘지 않는다.

L6에서 HyperCLOVA X는 "의미 감사"를 맡되 **권한 계층**이 적용된다:
LLM은 심각도를 올릴 수만 있고 결정론적 판정을 완화하지 못한다
(REVISE → LLM이 APPROVE라고 해도 REVISE 유지). 이 단조성이 깨지면 L6 전체가
무의미해지므로, `merge_supervision()`의 이 로직은 리팩터링 시에도 반드시 보존할 것.

L6가 결정론적 감사만으로 안 되는 이유: 수치는 근거에 실재하고 규칙 위반도 없는데
**설명이 틀린 경우**(예: 연금수령연차와 연금실제수령연차 혼동)는 규칙으로 못 잡는다.
이건 의미 판단이라 HyperCLOVA X가 필요하다.

**Exploration → Exploitation은 순차**다. 병렬로 만들지 말 것.
자격 검증 없이 수치를 비교하면 "총보수 최저인데 가입 불가능한 상품"을 추천하게 된다.

## 구현 완료 모듈 (수정 시 주의)

| 파일 | 내용 | 상태 |
|---|---|---|
| `pension_calc_functions.py` | 계산함수 15종 | 동작 검증 완료 |
| `trap_rules.py` | 함정 규칙 26종 + 감지 | 동작 검증 완료 |
| `numeric_verifier.py` | 수치 대조 검증 | 동작 검증 완료 |
| `citation_system.py` | 근거 인용 + 무결성 | 동작 검증 완료 |
| `supervisory_board.py` | 감독 4대 감사 + 의미 감사 + 계획 감사 | 동작 검증 완료 |
| `grounding_retrieval.py` | L0 사전 검색 + 조기 거절 | 동작 검증 완료 |
| `coverage_pipeline.py` | 슬롯·커버리지·타임아웃·trace | 일부 구현 |

## 미구현 — 이번에 만들 것

`coverage_pipeline.py`의 `build_answer()`가 주입받는 함수는 **7개**다.
그 중 2개는 이미 만든 모듈로 대체 가능하고, 5개는 실제로 새로 만들어야 한다.

**신규 구현 필요 (5개)**
- `extract_query_spec` — Function calling 스키마 (L1)
- `retrieve_hybrid` — pgvector + BM25 (L3)
- `slot_evidence_matcher` — 슬롯-근거 매칭
- `calc_params_builder` — 슬롯 → 함수 인자 **(없으면 계산함수 15종이 전부 호출되지 않음)**
- `answer_covers_slot` — 답변이 슬롯을 실제로 반영했는지 검사. `slot_evidence_matcher`와
  마찬가지로 키워드/엔티티 매칭으로 먼저 구현 가능

**기존 모듈 연결로 대체 (2개, 새로 만들지 말 것)**
- `generate_answer` — Supervisor 프롬프트 (L5'). 프롬프트 자체는 신규 작성이지만
  출력 후 검증은 아래 `verify_grounding` 대체재를 통과해야 함
- `verify_grounding` — **원래 인터페이스대로 새로 만들지 말 것.**
  `numeric_verifier.verify_numeric_grounding()`(수치 대조) +
  `supervisory_board.supervise_hybrid()`(의미 감사)로 이미 대체됐다.
  이 자리에는 두 함수를 순서대로 호출하는 얇은 래퍼만 작성할 것

추가로 `decide_answerability`에 **REFUSE 반환 경로가 없다**. 최고 가중치 지표가
"정보한계 대응"인데 거절 분기가 없는 것은 치명적이므로 반드시 추가할 것.

## 문서 원본 포맷 — Phase 2 착수 전 반드시 확인

**제공 문서는 확장자만 `.pdf`이지 실제로는 표준 PDF가 아니다.**
내부 구조는 **zip 압축파일이며, 페이지별 JPEG 이미지 + OCR 텍스트**로 구성돼 있다.

일반적인 PDF 텍스트 추출 라이브러리(pdfplumber, PyPDF2 등)로 열면 실패하거나
빈 텍스트만 나온다. Phase 2 착수 시 가장 먼저 할 일:

1. 샘플 파일 하나를 `zipfile`로 열어서 실제 내부 구조부터 확인할 것
   (`import zipfile; zipfile.is_zipfile(path)`로 먼저 판별 가능)
2. 내부에 이미 OCR 텍스트가 있다면 그것을 우선 사용 — 재OCR 불필요
3. OCR 텍스트가 없거나 품질이 낮으면 JPEG 페이지에 별도 OCR 적용 검토
4. 이 구조에 맞는 **커스텀 추출 파서**를 먼저 작성하고, 그 위에 청킹·메타데이터
   생성(문서 유형·엔티티·조항 위치 — `citation_system.py`의 `infer_doc_type`,
   `extract_locator`가 기대하는 필드와 맞출 것)을 얹을 것

실제 문서 파일을 저장소에 못 올리는 상황이면, 이 구조를 흉내낸 **mock zip
파일 하나**(임의 JPEG 1장 + 텍스트 파일 1개를 zip으로 묶은 것)로 파서 로직만
먼저 검증하고, 실제 파일은 나중에 팀원이 받으면 그때 교체 검증할 것.

## 도메인 함정 — 코드에 반영해야 할 것

`trap_rules.py`에 26종이 정리돼 있으나, 특히 다음은 로직에 직접 반영됨:

- **연금수령연차 ≠ 연금실제수령연차** — 전자는 수령한도, 후자는 퇴직소득세 감면율 결정
- **1,500만원 초과 시 전액이 과세 대상** (초과분만이 아님)
- **15% vs 16.5%는 지방소득세 포함 여부 차이** (상충 아님, 15 × 1.1 = 16.5)
- **구법 수치 문서 혼재** — 700만원/1200만원/4천만원은 개정 전 수치
- **판매 클래스 가입자격** — 총보수 최저 클래스가 가입 불가인 경우가 많음

## 금액 단위

계산함수 전체가 **만원 단위**다. 사용자 질의는 원 단위인 경우가 많으므로
입출력 경계에서 변환하고 내부는 만원으로 통일할 것. 혼동 시 1만배 오차가 난다.

## 코드 스타일

- 한국어 주석. 도메인 용어는 한국어 그대로 (연금수령한도, 세액공제 등)
- 계산 함수는 순수함수. 반환값 dict에 `source` 필드로 근거 문서 ID 포함
- 예외를 삼키지 말 것. 특히 감사·검증 실패는 반드시 think_trace에 기록
- 새 기능보다 미구현 연결이 우선. 마감이 촉박하다

## 테스트

- 계산함수는 회귀 테스트로 기대값 고정 (제공문서 원문 예시 재현 확인)
- 함정 규칙 추가 시 참고 질의 5개로 오탐 회귀 확인
  (과거 "정해지는"의 '해지'가 규칙에 걸리는 오탐 발생 이력 있음)
