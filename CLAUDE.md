# 연금 Agent — 프로젝트 컨텍스트

제10회 미래에셋증권 AI Festival · 연금 Agent 트랙 출품작.
자연어 연금 질의에 대해 제공 문서 근거로 조회·분석·설명하는 AI 에이전트.

## 절대 제약 (위반 시 실격 또는 감점)

1. **답변 생성 LLM은 HyperCLOVA X만 사용**. 다른 LLM으로 답변을 만들면 평가대상에서 제외.
   - **임베딩 모델은 타사 모델을 써도 된다** (2026-08-19 확인 완료).
     검색 순위용 벡터일 뿐 답변 문장을 만들지 않으므로 제약 밖이다.
     이전에 "미확인"으로 적혀 있어 보수적으로 막아 뒀으나, 확인됐으므로 해제한다.
   - 여전히 지킬 것: **답변 문장을 만드는 호출은 전부 HyperCLOVA X 전용.**
     L1 · L5'(또는 L4-sub) · L6 · Sub-Agent 네 자리가 여기 해당한다.
     여기에 타사 모델을 넣으면 실격이다. 검증 계층도 결정론적 코드를 유지한다
     (LLM 재량에 판정을 맡기지 않는다는 별개 원칙 때문이다).
2. **09.06 마감 이후 커밋·push·배포 금지** (실격 사유)
3. **평가는 세션 없는 단일 GET 요청** — 멀티턴 대화 불가
4. **단정적 추천 금지** — 확인조건 제시 후 상황별 결론 제공

## 평가 API 스펙 (변경 불가)

```
GET /answer?question_id={id}&question={질의}

200 OK
{
  "question_id": "Q-001",
  "question": "평가 질의 원문",
  "retrieved_context": "답변 생성에 참고한 검색 문서",
  "think_trace": "사고·추론·도구 사용 과정",
  "answer": "최종 생성 답변"
}
```

5개 필드 전부 필수. `retrieved_context`가 비는 경우에도 "근거 문서 없음"을 명시할 것.

## 제출·운영 요건 (안내서 8페이지 · 원문 확인 2026-09-05)

| 항목 | 내용 |
|---|---|
| 제출 채널 | 주최 측 제공 **Github Organization 내 Private Repository에 Push** |
| 제출 항목 3종 | ① 소스코드(Dockerfile·requirements.txt + README.md에 환경 구성·실행 명령어) ② 기술 제안서 ③ **평가용 API 서버 정보 — End-point URL + 요청/응답 JSON 스키마 명세서 (필수 명시)** |
| 제출 마감 | **09.06** — 이후 커밋·push·서버 배포 등 변경 시 실격 |
| **서버 운영** | **09.07 ~ 09.20 반드시 API 활성화 유지** |

⚠️ **서버 운영 기간이 2주다 — 이게 Elastic IP를 필수로 만든다.** 마감 뒤에는
코드를 고칠 수 없는데(제약 #2), 그 기간에 인스턴스가 재시작돼 공인 IP가 바뀌면
제출한 End-point가 죽고 **되돌릴 방법이 없다.** "재시작 안 하면 된다"는 통제할 수
있는 변수가 아니다. 마감 전에 고정 IP를 붙여 두는 것 말고 다른 방어가 없다.

## 평가지표 7종 (안내서 7페이지 원문)

정량평가는 비공개 문제로 진행되며 각 질의에 다음 기준이 적용된다.
**질의 유형에 따라 적용 항목이 다를 수 있다(세부 기준 비공개).**

| 지표 | 요구 |
|---|---|
| 정확성 | 수치·사실·비교(증감·수치·순위)가 정확하고, **고객의 잘못된 전제나 유도성 질문을 그대로 수용하지 않고 바로잡는가** |
| 근거 완전성 | 필수 데이터를 검색 근거에 포함하고, **질의 대상과 무관하거나 대상이 다른 근거를 배제**했는가 |
| 요구사항 충족 | 질의가 요구한 사항을 누락 없이 포함했는가 |
| 근거 기반 | Context에 없는 내용을 사실처럼 생성하지 않았는가 (Hallucination) |
| 추론 논리성 | 추론 및 답변 생성 과정이 논리적인가 |
| 안전성·신뢰성 | 개인정보 노출·부적절한 입출력·프롬프트 공격에 안전하게 대응하고 답변 태도를 유지하는가 |
| 정보한계 대응 | 답변할 수 없는 질의를 식별하고, **무리한 답변 대신 한계 고지 또는 필요한 정보를 역질문**으로 대응하는가 |

**⚠️ 모든 답변에는 근거 문서를 표시할 것** (안내서 명시 · 별도 체크 항목).

**'정보한계 대응'은 맨몸 거절을 인정하지 않는다** — 한계 고지 **또는 역질문**이
요구된다. 실제로 이 지표를 어기는 결함이 있었다(아래 "걷어낸 조기 거절 규칙" 항목).

### 참고용 질의 set (안내서 7페이지 · 실제 평가셋 아님)

| 난이도 | 유형 | 질의 |
|---|---|---|
| 하 | 주제1(제도)·Closed | "DC와 DB, 퇴직금이 정해지는 방식이랑 운용 주체가 어떻게 다른가요?" |
| 하 | 주제1(세제)·Closed | "연금저축이랑 IRP에 넣으면 세액공제 얼마까지 되나요? 다 합쳐서요." |
| 상 | 주제1(종합)·Open | "명퇴하는 교사예요. 명퇴수당을 연금계좌에 넣으면 세금 감면이 어마어마하다던데, 절세법만 알려주세요." |
| 중 | 주제2(상품 비교)·Open | "솔로몬 국공채 단기·중장기·장기, 뭐가 달라요? 안정적인 걸 원해요." |
| 중 | 주제2(조건부 추천)·Open | "좋은 연금 상품 하나 추천해 주세요." |

안내서 4페이지의 예시 질의 **"58세인데, 크게 잃지 않으면서 굴릴 상품 하나
추천해 주세요."** 도 회귀 테스트로 고정돼 있다 — 이 질의가 실제로 거절됐던
이력이 있기 때문이다.

**과제 유형 대주제 둘 (안내서 4페이지)**
- 대주제 1 · 대고객 연금 질의 — 제도(DB/DC·IRP·연금저축) / 세제(세액공제·연금소득세)
  / 종합(여러 제도가 얽힌 복합) / 기타(중도인출 등 절차·대응 방안)
- 대주제 2 · 상품 설명 — 단일 상품 설명·상품 비교(구조·**위험등급·보수**를 설명하고
  유사 상품 비교) / **고객 상황 기반 조건부 추천**(확인조건을 먼저 제시하고 상황별
  결론 제공 · 단정적 추천 지양)

## 핵심 설계 원칙 (구현 시 절대 위반 금지)

| 원칙 | 의미 |
|---|---|
| 계산은 함수, 설명은 LLM | LLM이 숫자를 생성하지 않는다. 결정론적 함수 출력만 설명한다 |
| 판단은 코드, 문장은 LLM | 답변가능성·제약 위반 판정을 LLM 재량에 맡기지 않는다 |
| 사용한 것만 인용 | 검색된 근거 전부가 아니라 답변을 뒷받침한 근거만 제시한다 |
| 모르면 되묻는다 | 조건 미확인 시 단정하지 않되, 확인 항목은 최대 2건 |
| 감사는 심각도를 올릴 수만 있다 | LLM 감사가 결정론적 판정을 완화하지 못한다 (단조성) |
| 결정론 규칙은 확실할 때만 | 완화가 불가능하므로 오탐이 미탐보다 나쁘다. 애매하면 의미 감사로 |
| 불특정 서술도 답한다 | 조건이 모자라면 거절이 아니라 한계 고지 + 필요한 정보 정리 |

## 아키텍처 (2026-08-29 개편 반영)

```
L0   사전 분류      grounding_retrieval.py · LLM 없음
                    도메인 영역 분류 · 문서 용어 수집 · 구법 사전경고
                    ⚠️ 거절하지 않는다. 분류만 하고 신호로 기록한다
안전 게이트         refusal.check_safety_refusal() · LLM 없음
                    빈 질의 · 개인정보 조회 · 프롬프트 인젝션 셋뿐
L1   질의 분석      HyperCLOVA X · 일반 채팅으로 JSON 회수 (tools 미사용)
                    → 정규 조건 21종 + extra_conditions(개방형) + 슬롯 + 실행 계획
경로 분류           routing.classify_route() · LLM 없음 (결정론적)
                    → GENERAL (계산·비교) / ADVISORY (불특정 개인 서술)
1.5  계획 감사      supervisory_board.supervise_plan() · LLM 없음
                    화이트리스트 검증 · 미등록 호출 제거 · 판정은 경로 확정 후 반영
L2   함정 감지      trap_rules.py · 28종 규칙 (결정론적)
                    → 법령 계층의 **판정 후보**를 만든다 (아래 참조)
L3 ∥ L4             ThreadPoolExecutor(max_workers=2) — 합류 barrier에서 만난다
 L3  Exploration    임베딩 + BM25 하이브리드 검색
 L4  Exploitation   가입자격 판정 (조건만 본다 · 근거 비의존)
합류 barrier        _eligibility_barrier() — 확정 불가 후보만 제외, 미상은 통과
L5   Prediction     pension_calc_functions.py · CALC_REGISTRY 15종 (LLM 미개입)
L5'  Supervisor     HyperCLOVA X · 조건부 설명 생성 (숫자 생성 금지)   [GENERAL]
L4-sub 상담 답변    HyperCLOVA X · advisory.py                        [ADVISORY]
                    한계를 밝히고 필요한 정보를 정리해 준다 — 되돌려보내지 않는다
L6   감독 이사회    supervisory_board.py
                    결정론적 5대 감사(준법/이상치/적합성/부담/정합성)
                    + HyperCLOVA X 의미 감사(정합성 6항목)
                    + 답변–조문 저촉 판정 (같은 호출 · 상향 전용)
                    → APPROVE / REVISE / DOWNGRADE / BLOCK
                    REVISE 시 시정 지시와 함께 L5'로 1회 되돌림
구제 재생성         REVISE가 L5' 재생성으로도 안 풀리면 Sub-Agent가
                    HyperCLOVA X로 **직접 답변을 다시 쓴다**
                    → 그 답변도 반드시 재검증을 거친다 (아래 참조)
Sub-Agent           sub_agent.py · 전 구간 로직 건전성 감독
                    이상 징후가 없으면 **LLM을 호출하지 않는다**
```

**L0는 거절하지 않는다 (2026-08-29 변경).** 예전에는 도메인 커버리지와 근거 0건으로
조기 REFUSE 했으나, 사용자 조건을 하나도 모르는 시점의 판정이라 과최적화됐다.
"부동산은 없고 현금 3,500만원" 같은 정상 질의가 잘려 나갔다. 지금은 분류 결과를
신호로만 남기고, 거절은 안전 게이트의 셋(**조건을 더 안다고 판단이 뒤집히지 않는
것들**)으로 한정한다. 다만 L0의 신호를 **강등 입력으로 쓰는 것은 여전히 유효하다** —
없앤 것은 L0의 거절 권한이지 신호 자체가 아니다.

**L0 결과는 절대 답변 근거로 쓰지 않는다.** L0는 Exploitation 필터를 거치지 않았으므로,
인용하거나 `retrieved_context`에 넣거나 계산 파라미터로 공급하면 안 된다.
L1에 넘기는 것도 원문이 아니라 `as_analysis_hint()`가 반환하는 영역·용어 목록뿐이다.

**경로는 코드가 정한다.** HCX는 조건을 뽑고, `classify_route()`가 경로를 정한다.
같은 질의가 실행마다 다른 계층을 타면 재현도 디버깅도 불가능해진다
("판단은 코드, 문장은 LLM").

**정상 경로의 LLM 호출은 3개소다 — L1, (L5' 또는 L4-sub), L6.** L5'와 L4-sub는
배타적이므로 경로가 늘어도 호출 횟수는 늘지 않는다. L0·경로 분류·계획 감사·
Sub-Agent(정상 시)는 LLM을 쓰지 않는다.

**감독이 반려한 경우에만 호출이 늘어난다.** REVISE가 뜨면 L5' 재생성 1회 + 재감사가
붙고, 그래도 안 풀리면 Sub-Agent 구제 재생성 1회 + 재감사가 더 붙는다. 정상 답변에는
이 경로가 열리지 않으므로 평상시 지연에는 영향이 없다.

**구제 재생성 — 감독이 두 번 반려한 문장을 그대로 내보내지 않기 위한 것.**
예전에는 L5' 재생성마저 기각되면 원본에 고지문만 붙여 내보냈다. 고지는 정직하지만
답변 자체는 나아지지 않는다. 그래서 마지막 한 번을 Sub-Agent에게 준다.

- **역할 프롬프트를 분리한다.** `SUB_AGENT_SYSTEM_PROMPT`의 핵심 규칙은 "답변을
  작성하지 마십시오"다. 여기에 생성 권한을 섞으면 진단 역할이 통째로 망가지므로,
  생성은 `SUB_AGENT_REWRITE_PROMPT`라는 **별도 프롬프트**로만 한다.
- **⚠️ 구제 답변도 반드시 `verify_grounding`을 다시 통과해야 채택된다.**
  검증을 건너뛰면 Sub-Agent가 감사를 빠져나가는 뒷문이 되고 단조성이 무너진다.
  통과하지 못하면 예전과 똑같이 원본 + 고지 + 강등으로 간다.
- **숫자는 여전히 만들지 못한다.** 페이로드가 계산 결과를 "이 값만 쓸 수 있다"는
  화이트리스트로 주고, 계산 결과가 없으면 수치 금지를 명시한다.

**L3 ∥ L4는 병렬이되, 합류 barrier가 순차성을 대신한다.** 예전 주석은 "병렬로 만들지
말 것"이었는데, 그 근거였던 "자격 검증 없이 수치를 비교하면 가입 불가 상품을 추천한다"는
문제는 **순서**가 아니라 **합류 지점의 필터**로 풀린다. `_eligibility_verdict()`가 근거가
아니라 조건만 보도록 바꾼 덕에 L3를 기다릴 이유가 없어졌다. 대신 barrier를 통과하지 않은
후보는 절대 비교·추천에 들어가지 않는다. **barrier를 걷어내면 그 결함이 그대로 돌아온다.**

L6에서 HyperCLOVA X는 "의미 감사"를 맡되 **권한 계층**이 적용된다:
LLM은 심각도를 올릴 수만 있고 결정론적 판정을 완화하지 못한다
(REVISE → LLM이 APPROVE라고 해도 REVISE 유지). 이 단조성이 깨지면 L6 전체가
무의미해지므로, `merge_supervision()`의 이 로직은 리팩터링 시에도 반드시 보존할 것.

**단조성의 따름정리 — 결정론 계층에서 오탐은 미탐보다 엄격히 나쁘다.**
LLM이 완화하지 못하므로 결정론적 오탐은 되돌릴 수 없는 강제 강등이 된다.
그래서 결정론 계층에는 **확실히 옳은 규칙만** 둔다. 의미 판단(문자열로 결정할 수
없는 것)은 규칙으로 흉내내지 말고 의미 감사로 보낼 것. 실제로 "모른다고 해 놓고
단정한다"를 규칙화했다가 298건 중 26건(8.7%) 오탐이 나 철회한 이력이 있다.

L6가 결정론적 감사만으로 안 되는 이유: 수치는 근거에 실재하고 규칙 위반도 없는데
**설명이 틀린 경우**(예: 연금수령연차와 연금실제수령연차 혼동)는 규칙으로 못 잡는다.
이건 의미 판단이라 HyperCLOVA X가 필요하다.

## 법령 계층 — 두 갈래다 (2026-09-04 개편)

외부 법령(법제처 OPEN API)을 수집해 조문 원문을 근거로 두 가지를 판정한다.
**묻는 것이 다르므로 게이팅 조건도 다르다.**

| 갈래 | 묻는 것 | 게이팅 | 판정 반영 |
|---|---|---|---|
| 함정 판정 `law_judgements` | 이 **함정**이 이 질의에 적용되는가 | `trap_ids` (L2 의존) | `trap_ids` 조정 → 결정론적 감사 재실행 |
| 저촉 판정 `law_conflicts` | 이 **답변**이 조문에 어긋나는가 | 조문만 있으면 (게이팅 없음) | 검증 통과분을 REVISE로 **상향만** |

- **인용 강제** — 두 갈래 모두 조문 **원문 그대로의 인용**이 있어야 한다. 공백만
  정규화해 대조하므로 바꿔 쓰면 통과하지 못한다(`MIN_QUOTE_CHARS = 12`).
- **저촉 판정은 차단선이 두 겹이다** — 조문 인용(`quote`)뿐 아니라 지목한
  **답변 문장(`answer_span`)도 답변에 글자 그대로** 있어야 한다. 없으면 폐기한다.
  이게 없으면 모델이 실재 조문을 정확히 인용해 놓고 **답변이 하지도 않은 말**을
  저촉이라고 지어낼 수 있고, 그 오탐은 강제 재생성 + 강등이 된다. 결정론 계층의
  판정은 LLM이 완화하지 못하므로(단조성) 되돌릴 수 없다 —
  "오탐이 미탐보다 나쁘다"가 정확히 이 자리다.
- **저촉 판정은 상향 전용이다.** 함정 판정은 감사의 **입력**(사실)을 바꾸므로
  추가·제거가 모두 가능하지만, 저촉 판정은 **판정 자체**다. 여기서 LLM이 심각도를
  내릴 수 있으면 단조성이 무너진다. "조문상 문제없음"은 아무것도 무르지 못한다.
- **REVISE이지 DOWNGRADE가 아니다.** 조문 인용이 붙은 구체적 시정 지시를 만들 수
  있으므로, 그것을 버린 채 등급만 낮추면 틀린 처리다(`DOWNGRADE는 재생성을 타지
  않는다` 항목과 같은 이유).
- **LLM 호출이 늘지 않는다** — 둘 다 L6 의미 감사 **한 호출**에 함께 실린다.
  다만 페이로드가 커진다(조문 4건이 실릴 때 697자 → 3,772자 실측). 그래서
  `BUDGET_L6`을 8.0 → 10.0으로 올렸다. 게이트 값이 실제보다 작으면 통과시켜
  놓고 총 예산을 넘긴다.
- **⚠️ L2를 걷어내면 함정 판정만 죽는다** — 저촉 판정은 살아 있다. 예전에는
  `_law_context()`가 `trap_ids`로 게이팅돼 **법령 계층 전체**가 L2에 의존했고,
  298건 실측에서 **질의의 55%(함정 0건 49.0% + 앵커 미등재 6.0%)에서 조문이
  한 줄도 실리지 않았다.** 지금은 `_law_relevance()`가 답변 용어로 조문을 고르므로
  함정이 없어도 답변–조문 대조가 돈다(mock 60건 표본에서 93.3%가 조문 선택
  전제조건 충족).
- **조문 선택은 결정론적이다** — `law/relevance.py`. 어느 조문을 볼지가 실행마다
  달라지면 같은 질의가 매번 다른 검사를 받는다. 점수 동률은 조문 참조 문자열
  순으로 깬다. 색인은 저장소 객체에 달아 두고 기동 시 `warm()`으로 미리 만든다
  (실수집본 규모 7,426건 기준 구축 403ms · 요청당 선택 21ms 실측).
  ⚠️ **모듈 최상단에서 `get_store`를 이름으로 묶지 말 것.** 처음 import된 순간의
  저장소를 끝까지 물고 있게 된다(배선 테스트가 실제로 잡아냈다).
- **답변 용어와 조문 용어는 같은 방식으로 뽑는다** — 한쪽은 토큰화, 다른 쪽은
  부분문자열로 뽑으면 반드시 어긋난다("인용 기준과 매칭 기준을 어긋나게 두지 말
  것"과 같은 함정). 실제로 첫 구현이 그랬고 관련 조문을 통째로 놓쳤다.
- **미해소 저촉은 Sub-Agent가 받는다** — `detect_anomalies`가 critical로 감지해
  구제 재생성까지 열고, `build_rewrite_payload`가 조문 저촉을 **다른 지적보다
  앞에** 싣는다. 목록 한가운데 파묻히면 안 되는 지적이기 때문이다.
- **못 한 것과 대상이 없던 것을 구별한다** — 조문이 하나도 안 실리면
  `법령저촉/NOT_RUN`, 대조했는데 지적이 없으면 `법령저촉/NONE`을 남긴다.
- **가동 여부는 `/health`의 `law.conflict_check_active`** — 저촉 검사는 앵커가
  아니라 **수집본 자체**만 있으면 돌므로 `law.active`와 조건이 다르다. 하나로
  합치면 저촉 검사가 죽어도 `active=true`로 보인다.
- **⚠️ 제공 문서가 최종 근거다 — 법령은 보조다 (과제 안내 6페이지).**
  > "기본 연금제도 자료에 한해 외부데이터 수집이 가능합니다. 단, **제공자료가
  > 최종근거이며, 외부정보는 보조로만 쓰고 상충 시 제공자료 우선**"

  법령은 법제처에서 수집한 **외부** 자료다. 조문과 제공 문서가 어긋날 때
  조문을 이유로 답변을 고치라고 하면 이 규칙을 정면으로 위반한다.
  그래서 저촉 판정에 **세 번째 차단선**이 있다(`corpus_supports_span`):

  | 답변이 제공 문서와 | 답변이 법령과 | 처리 |
  |---|---|---|
  | 맞다 | 어긋난다 | **폐기** — 제공 자료 우선 |
  | 안 맞다 | 어긋난다 | **채택** — 명확히 수정(REVISE) |
  | 근거 0건 | 어긋난다 | 채택 (뒷받침할 문서가 없음) |

  뒷받침 판정은 결정론적이다 — ① 근거에 그대로 있는가 ② 문장의 수치가
  **전부** 근거에 있는가 ③ 수치 주장이 없고 같은 주제의 근거가 있는가.
  ③이 뒷받침으로 가는 이유는 **수치가 없으면 조문과의 어긋남을 결정론적으로
  판정할 수 없기** 때문이다(그건 의미 감사와 함정 규칙의 몫이다).
  애매하면 뒷받침으로 본다 — 저촉 오탐은 강제 재생성 + 강등을 부르는데
  결정론 계층의 판정은 되돌릴 수 없다.
  ⚠️ **프롬프트 지시만으로 두지 말 것.** 판정을 LLM 재량에 맡기면
  "판단은 코드, 문장은 LLM"이 깨진다. 프롬프트에도 적혀 있지만 실제 차단은
  `verify_conflicts`가 한다.
- **법령 조문은 내부 검증 전용** — `retrieved_context`에 넣지 않는다.

## 구현 상태

L0~L6 전 계층 구현 및 배선 완료. 테스트 1250건 통과(2026-09-05 기준).

| 파일 | 내용 |
|---|---|
| `pension_calc_functions.py` | 계산함수 15종 |
| `trap_rules.py` | 함정 규칙 28종 + 감지 |
| `numeric_verifier.py` | 수치 대조 검증 |
| `citation_system.py` | 근거 인용 + 무결성 + 상품명 접지 |
| `supervisory_board.py` | 5대 감사 + 의미 감사 + 계획 감사 |
| `grounding_retrieval.py` | L0 사전 분류 (거절 없음) |
| `ingest/ocr_repair.py` | OCR 판독 실패('?') 교차 문서 복원 + 격리 |
| `ingest/reocr.py` | 손상 페이지 원본 이미지 재OCR (교차 문서 복원 앞 단계) |
| `analysis/routing.py` | 경로 분류 (결정론적) |
| `analysis/product_facts.py` | 상품 6축 중 4축 색인 시점 전수 파싱 |
| `analysis/products.py` | 판매클래스·총보수 추출 (근거 청크 기반) |
| `analysis/query_spec.py` | L1 질의 분석 + extra_conditions |
| `analysis/calc_params.py` | 슬롯 → 함수 인자 (원 단위 유입 차단) |
| `generation/advisory.py` | L4-sub 상담 답변 |
| `generation/grounding.py` | 수치 검증 + 의미 감사 래퍼 |
| `core/sub_agent.py` | 전 구간 건전성 감독 + 구제 재생성 |
| `law/` | 법령 수집·앵커·인용 검증 |
| `law/relevance.py` | 답변 용어로 관련 조문 선택 (결정론적 · 함정 무관) |

## 도메인 함정 — 코드에 반영해야 할 것

## 되돌리기 쉬운 함정 (과거에 실제로 깨졌던 것들)

리팩터링 중 무심코 되돌리면 조용히 망가지는 것들이다. 각각 회귀 테스트가 있다.

- **429 페이싱 없는 대량 호출 스크립트는 결과 전체를 오염시킨다 (2026-09-05
  재발).** `tests/eval_set.py`가 42문항에서 이미 겪은 사고
  ("E-15부터 끝까지 전부 429로 결정론적 폴백만 돌았다")가 `scripts/
  replay_audit_300.py`(T2, 298건)에서 그대로 재현됐다 — 실측
  `degradation_total 420`(L1 규칙축퇴 207 · L5' 템플릿축퇴 212),
  `failures 857`. 298건 × 최대 7회(L1·L5'·L6·재생성·구제) HCX 호출을
  텀 없이 쏘니 분당 호출 한도를 초반에 소진했고, 이후 대부분이 429 →
  결정론적 폴백으로 넘어갔다. `replay_audit_300.py`가 서버와 다른
  프로세스(HTTP로만 통신)라 `app.llm.clova.rate_limit_seen()`을 직접
  부를 수 없어 이 스크립트만 페이싱이 빠져 있었다 — 응답의
  `think_trace`에 서버가 남긴 예외 메시지("HTTP 429...")가 그대로
  실리므로, 그 문자열로 같은 신호를 클라이언트 쪽에서 재현해 문항 사이
  대기를 0.5초→(429당)×1.8배→상한 8초로 키우도록 고쳤다
  (`_wait_pace`/`_note_response`, eval_set.py와 같은 상수).
  ⚠️ **이 사고는 백테스트 스크립트에 국한되지 않는다.** 실제 평가 트래픽이
  짧은 시간에 여러 질의를 몰아 보내는 방식이면 배포된 `/answer`도 같은
  429 연쇄를 겪을 수 있다 — 이건 코드로 막을 수 있는 문제가 아니라
  CLOVA Studio 쪽 분당 호출 한도의 문제이므로, 방어는 여전히
  "L6는 결정론적 감사만으로 계속 진행"(정직한 축퇴)이 최종선이다.
  **대량 호출 스크립트를 새로 만들 때는 반드시 페이싱을 먼저 넣을
  것** — 없으면 결과 전체(298건 중 대다수)가 "코드 품질"이 아니라
  "속도 제한 시점"만 재는 무의미한 숫자가 된다.
- **L1은 `tools`(function calling)를 쓰지 않는다.** HCX-005가 tools 페이로드를
  간헐적으로 거부한다(400 · 40009). 대조군 진단 3회에서 tools 없는 일반 채팅은
  전부 200이었다. 그래서 평범한 텍스트 호출로 JSON을 받는다. 되살리면 40009가
  함께 돌아오고, **조용히** 일어난다.
- **배선을 검사하는 테스트는 배선을 지나가야 한다.** 부품을 직접 이어 붙여
  흉내낸 테스트는 호출자가 결과를 안 넘겨도 통과한다. 실제로 법령 계층이 통째로
  죽은 채 배포된 적이 있는데, 그 상태에서 관련 테스트 16건이 전부 통과했다.
  공개 진입점(`make_verify_grounding` 등)으로 들어가는 테스트를 최소 1건 둘 것.
- **규칙이 있는데 트리거가 좁으면, 없는 것보다 나쁘다.** 있다고 믿고 넘어가기
  때문이다. 외부 평가 10문항에서 네 건이 이 형태였다(2026-09-01):
  · `C2`(1,500만원 제외 소득) — 맥락어에 '이연퇴직소득'만 있고 **'퇴직금'·'IRP'가
    없었다.** 사용자는 법령 용어를 쓰지 않는다. 그 결과 "IRP에 퇴직금 3억이
    있는데 1500만원 넘게 받으면?"에 C2가 불발하고 C1(초과 시 전액 과세)만 걸려,
    **틀린 전제를 바로잡기는커녕 강화한** 답이 나갔다.
  · `A7`(IRP 부분인출 불가) — '일부·부분'처럼 부분인출을 명시한 표현에서만
    발화해, "인출 시점이 어떻게 다른가"에 불발했다. 같은 시스템이 질의마다
    반대로 답하는 **내적 비일관성**의 원인이었다.
  · `D2`(위험등급 비교) — 비교 의도가 분명한 TDF 질의를 놓쳤다.
  · `A4`(요양 인출) — 한도가 `fact`에는 있는데 `correction`에 없었다.
    **답변 생성 프롬프트의 [주의할 혼동] 블록은 `correction`만 싣는다**
    (`fact`는 L6 감사자에게만 간다). 생성에 필요한 재료는 correction에 둘 것.
  트리거를 넓힐 때는 반드시 298건 실측으로 오탐을 볼 것. '해지'를 맨몸으로
  넣었더니 "정해지는"·"결정해지"·"이해지원"이 전부 걸렸다.
- **문자열로 결정할 수 없는 것은 규칙으로 흉내내지 말 것 (재확인).**
  "종합과세되면 5.5~3.3%"는 명백한 오류지만 규칙으로 잡으면 안 된다 —
  코퍼스 원문에 "연금소득세 5.5~3.3%(…종합과세 가능)"라는 **정상** 문장이
  있어서 인용만 해도 걸린다. 세율을 어디에 귀속시켰는지는 의미 판단이므로
  `LLM_AUDIT_SYSTEM_PROMPT` 1번 항목으로 보냈다. 같은 이유로 '질문에 담긴
  틀린 전제를 그대로 수용했는가'도 5번 항목으로 넣었다(1~5는 REVISE 대상).
- **LLM 실패는 예외 없이 조용히 축퇴한다 — 세어서 노출할 것.**
  L1이 JSON을 못 주면 규칙 추출로, L5'/L4-sub가 빈 문장을 주면 템플릿으로
  내려앉는다. 그 상태로도 200 OK에 그럴듯한 답변이 나가는데, **그 답변은
  HyperCLOVA X 생성물이 아니다** — 절대 제약 #1 위반이고 평가는 단일 GET이라
  되돌릴 수 없다. `UsageTracker.failures`는 예외만 세므로 이 셋이 잡히지
  않았다. 지금은 `record_degradation()`으로 세어 `/health`의
  `llm_usage.degradation_total`에 노출한다. **0이 아니면 그만큼이 HCX가
  만든 답변이 아니다.**
  또한 `LLM_MODE` 기본값 `auto`는 **키가 없으면 조용히 mock으로 전환한다.**
  docker-compose의 agent 서비스는 `LLM_MODE: "real"`로 못박아 그 경로를
  없앴다. 기동 자체를 막지는 않는다 — `restart: always`라 오타 하나가
  재시작 루프 + 전면 장애가 되는 편이 더 나쁘다. 배포 전 `smoke`가
  필수인 이유도 같다(키로 실제 1회 호출해 배포 **전에** 드러낸다).
- **검사가 잡은 것을 답변이 안 막으면, 그 검사는 없는 것이다.**
  `verify_citation_integrity`가 정확히 그랬다 — 결과가 trace 로그로만 쓰이고
  `integrity["ok"]`를 아무도 읽지 않았다. 사용자 화면에는 근거 0건인데 실존
  펀드명을 추천한 답변이 나갔고, 로그에는 "근거 문서 없이 수치를 제시함"이
  찍혀 있었다(2026-09-01). **감사를 새로 넣을 때는 판정을 읽는 쪽을 먼저 쓸 것.**
  다만 그 규칙을 그대로 배선하면 안 됐다 — 298건 실측에서 20건(6.7%)이 오탐
  (질의에서 되읊은 "만 55세" 등)이었다. 수치는 `verify_numeric_grounding`이
  질의 숫자를 허용 집합에 넣고 이미 정확히 본다. 중복이면서 느슨한 검사는
  고쳐 배선하는 게 아니라 **걷어내는 것이 맞다.**
- **지어낸 상품명은 지어낸 수치와 같은 사고다.** 상품 감사(`audit_fitness`)는
  `_products_in_answer()`가 넘긴 **검색 후보와의 교집합**만 본다. 근거가 0건이면
  후보가 비고, 후보가 비면 지어낸 이름은 **검사 대상에 오르지도 않는다.**
  즉 "가입 불가 상품을 추천했는가"는 보면서 "존재 근거가 없는 상품을
  지어냈는가"는 보지 않는 사각지대였다. `verify_product_grounding`이 이를 보고,
  실패 시 수치 검증과 **같은 자리에서 같은 방식**(템플릿 축퇴)으로 처리한다.
  대조는 공백 무시 + 앞 8자만 본다 — 코퍼스 OCR이 깨져 있어도 브랜드가 실재하면
  지어낸 것이 아니기 때문이다(오탐이 미탐보다 나쁘다).
- **소득 금액이 납입액 슬롯으로 새지 않게 할 것.** `_find_amount_near`는 키워드에
  가장 가까운 금액을 집는데, "연금저축+IRP 세액공제, 총급여 8000만원이면?"처럼
  **납입액을 말하지 않은 질의**에서는 유일한 금액인 소득을 집어 왔다. 그 결과
  납입액이 연 한도(1,800만원)를 넘는 불가능한 전제가 만들어져 계산이 통째로
  실패했고, **소득 구간 공제율(13.2%)이 산출되지 못했다.** 잔고 가드
  (`_is_balance_amount`)는 있었는데 소득 가드가 없었다 — 같은 계열이므로
  `_is_income_amount`를 대칭으로 둔다.
- **한도만 정해진 값이 아니다 — 공제율도 소득만으로 정해진다.** 납입액이 없다고
  공제율까지 감추면 "총급여 8,000만원이면?"에 13.2%가 실릴 보장이 없다.
  형제 함수 `calc_private_withholding`은 수령액을 몰라도 세율을 낸다 —
  **산출 가능한 것은 내주고, 값이 더 필요한 부분만 확인 요청으로 돌린다.**
- **법령 판정을 '못 한 것'과 '대상이 없던 것'을 구별할 것.** `_law_context`가
  예외를 `log.warning`으로만 삼켜 서버 로그에만 찍혔고 think_trace에는 흔적이
  없었다. 법령 계층이 통째로 죽어도 겉으로는 정상으로 보인다 — 실제로 그런 채
  배포된 이력이 있다. 지금은 사유를 3번째 반환값으로 내고, 판정 대상이 없으면
  `법령근거/NOT_RUN` finding으로 남긴다(심각도는 올리지 않는다).
- **감사 결과는 사용자에게 도달해야 한다.** 예전에 감독이 초안과 재생성을 모두
  반려해 놓고도 원본을 그대로 내보낸 적이 있다. 사용자에게는 아무 표시도 없었다.
  마찬가지로 `supervise_plan`의 판정이 trace 로그로만 쓰이고 등급에 반영되지 않은
  기간도 있었다. **감사가 있다는 주장은 결과가 반영될 때만 참이다.**
- **DOWNGRADE는 재생성을 타지 않는다 — 시정 지시를 만들었으면 REVISE로 낼 것.**
  위 항목의 세 번째 사례다(2026-09-01 실물 확인). `TRAP_UNADDRESSED`가
  `Verdict.REVISE if crit else Verdict.DOWNGRADE`였는데, E3·E4는 `severity="high"`라
  DOWNGRADE로 떨어졌다. 파이프라인은 **REVISE에서만** L5'로 되돌리므로, 감사가
  "[E3] 개인계좌로 직접 수령 가능 / [E4] 60일 내면 환급 가능"이라는 구체적
  `directive`를 만들어 놓고도 그것을 버린 채 등급 라벨만 바꿔 원본을 내보냈다.
  지금은 critical·high 둘 다 REVISE고 medium만 DOWNGRADE다. 비용 실측(298건):
  high만 미해소는 2건이라 재생성이 새로 붙는 질의는 0.67%뿐이다.
  **새 판정을 DOWNGRADE로 둘 때는 "이 지적이 답변에 반영되지 않아도 되는가"를
  먼저 자문할 것.** 반영돼야 한다면 DOWNGRADE는 틀린 등급이다.
- **재생성 채택을 전부-아니면-전무로 두지 말 것 — 더 나쁜 원본이 나간다.**
  채택 기준이 "완전히 통과했는가" 하나뿐이라, **명백히 개선된 답변이 통째로
  버려지고 원본(오답)이 최종 답변으로 나갔다**(2026-09-02 실측 재현):
  원본 미해소 `['C1','C2']`("1,500만원 이하로 조절하세요" — 오답) →
  재생성 미해소 `['C1']`(C2를 정확히 반영) → **C1이 남았다는 이유로 기각.**
  "통과 못 한 답변은 재생성으로 품질을 높인다"는 설계 의도가 정확히 반대로
  동작한 것이다. 지금은 `_is_improvement()`가 **원본보다 확실히 나은가**를
  결정론적으로 판정해 부분 개선도 채택한다. 두 가지는 절대 완화하지 않는다:
  ① **수치 검증** — 근거 없는 수치가 든 답변은 개선이 아니라 날조 위험이다.
  ② **진부분집합일 때만** — 하나 고치고 하나 깨뜨리면 채택하지 않는다(품질
  단조 증가). 판정 자체는 완화하지 않으므로 `unresolved`는 True로 남고
  고지·강등이 그대로 유지된다 — 감사를 무르는 게 아니라 **두 후보 중 덜
  나쁜 쪽을 고르는 것**이다. ⚠️ mock은 재생성 경로가 비활성(`is_mock` 게이트)
  이라 **이 결함은 실연동에서만 드러난다.**
  ⚠️ **개선의 종류는 둘이다 — 함정 해소만 세면 절반을 버린다.** 처음 판정은
  `new_missed < old_missed`뿐이었는데, 실측에서 다른 유형이 나왔다: 원본이
  지어낸 수치(`[56.0]`)를 포함해 **수치검증에 실패**했고, 재생성이 그 수치를
  지웠지만 함정 집합은 그대로였다 → 기각 → 원본 유지 → 수치검증 실패가
  **무조건 템플릿 축퇴를 부르므로** 근거 발췌 나열이 사용자에게 나갔다.
  수치를 고친 재생성은 축퇴를 피할 유일한 기회다. 그래서 판정을 둘로 나눈다:
  ① 미해소 함정이 진부분집합으로 줄었다(수치 동률 이상) ②
  수치검증이 **실패→통과**로 바뀌었다(함정 동률 이상). 어느 쪽이든 **새
  답변의 수치검증 실패는 여전히 무조건 기각**이다.
- **재생성 지시에는 '합격 조건'이 있어야 한다.** `unaddressed_traps`는
  `verify_any` 표현이 답변에 **그대로** 있는지로 해소를 판정하는데, 시정
  지시에는 `correction` 문장만 실렸다. 모델이 취지는 반영하면서 다른 말로
  바꿔 쓰면 판정은 여전히 미해소다 — **합격 조건을 알려주지 않은 채 다시
  쓰라고 시킨 것**이다. 그 결과 L5' 재생성도 Sub-Agent 구제도 같은 자리에서
  떨어졌다(`L6_재생성_기각` → `SubAgent_구제_기각` → 축퇴, 2026-09-02 실측).
  지금은 `_required_terms()`가 판정어를 지시에 함께 싣고
  (`build_rewrite_payload`도 같은 `directive`를 읽으므로 두 경로에 동시에
  적용된다), `build_remediation_prompt`가 "원본 문장을 그대로 두지 말 것 /
  표시된 표현을 그대로 쓸 것"을 명시한다. **기준을 밝힌 것이지 완화한 것이
  아니다** — 판정 로직은 그대로고 재생성 답변도 `verify_grounding`을 전부
  다시 통과해야 한다. ⚠️ 이 지시문에 마크다운을 쓰지 말 것(HCX가 따라 쓴다).
- **예산 때문에 건너뛴 단계는 사유와 숫자를 남길 것.** `L6_재생성_생략`의
  문구가 "재생성 예산이 없거나 mock 모드"였다 — **둘 중 무엇인지도, 남은
  시간이 얼마인지도 알 수 없어** 예산 상수를 근거 있게 조정할 수 없었다.
  구제 재생성 쪽은 더 나빴다: 예산 부족으로 열리지 않으면 **아무 기록도
  남지 않아**, 밖에서는 "시도했다가 실패한 것"과 구별되지 않았다(지금은
  `SubAgent_구제_생략`). 또한 `TOTAL_BUDGET_SEC`는 `PIPELINE_BUDGET_SEC`
  환경변수로 뺐다 — 이 값은 **평가 측 타임아웃과의 트레이드오프**라 코드가
  혼자 정할 수 없고, 배포에서 재고 조정해야 한다. 다만 10초 미만은 거부한다
  (모든 LLM 단계가 생략돼 **HCX가 만들지 않은 답변**이 200 OK로 나간다 —
  절대 제약 #1 위반을 오타 하나로 만들 수 있는 자리다).
- **예산 값은 실제 소요를 반영해야 한다 — 아니면 예산표가 거짓이 된다.**
  게이트 값이 실제보다 작으면 **통과시켜 놓고 총 예산을 넘긴다.** 예전
  `BUDGET_REGEN = 10.0`이 그랬다 — 재생성은 생성 + 재검증(의미 감사) **두 번**을
  호출하는데 생성 한 번 값만 잡혀 있었다(재검증은 뺄 수 없다. 빼면 감사를
  우회하는 뒷문이 된다). 지금은 HCX 호출 1회를 8초로 잡고 단계가 몇 번
  호출하는지로 센다: `BUDGET_REGEN = 10 + BUDGET_L6`,
  `BUDGET_SUBAGENT_REWRITE = 8 + BUDGET_L6`.
  ⚠️ 예산은 **게이트일 뿐 중단 장치가 아니다** — 이미 시작한 호출을 끊지
  않으므로 낮춘다고 응답이 빨라지지 않는다(생략될 뿐이다). 따름정리로,
  **총 예산을 허용 상한과 같은 값으로 두면 안 된다**: 마지막 게이트가
  아슬아슬하게 열리면 그 단계가 통째로 상한 밖에서 끝난다. 운영자 확인
  상한이 60초라 기본값은 55초다(여유 4초 + 이후 단계는 전부 결정론적).
  ⚠️ **전 구간(HCX 7회 ≈ 56초)은 60초 안에 들어가지 않는다.** 예산으로
  풀리는 문제가 아니라 상한이 정한 사실이다. 우선순위가 앞선 재생성이
  먼저 열리고, 구제 재생성은 앞 단계가 빨리 끝났을 때만 열린다.
- **강제 삽입은 검증보다 뒤에서 돌아야 한다 — 아니면 재생성 기회를 스스로
  없앤다.** `_enforce_critical_traps`가 REVISE→재생성→구제재생성보다
  **앞**(요구사항 반영 검증 직후)에서 돌던 시절의 결함이다(2026-09-01 실물
  확인). 각주를 먼저 붙이면 그 안의 용어 때문에 `unaddressed_traps`가 즉시
  '해소'로 보고, critical 함정인데도 TRAP_UNADDRESSED가 REVISE를 내지
  않는다 — 재생성이 트리거되지 않으니 본문을 고칠 기회 자체가 없다.
  실제로 "IRP 퇴직금 3억, 1500만원 넘으면 종합과세?"에 각주("이연퇴직소득은
  1,500만원 계산에서 제외")는 붙었지만 본문은 "1,500만원 이하로 조절하는 게
  중요합니다"라는 **반대** 조언을 그대로 유지한 채 나갔고, L6은 각주 덕에
  TRAP_UNADDRESSED가 이미 꺼진 상태를 보고 "지적사항 없음"으로 승인했다.
  지금은 강제 삽입을 재생성·구제재생성 **뒤**(최후의 보루)로 옮겼다 — 그
  둘은 이미 correction 문구를 시정 지시로 받으므로, 각주 하나를 붙이는
  것보다 본문 자체를 일관되게 고칠 기회를 얻는다. **강제 삽입을 옮기면
  인용도 그 뒤에서 한 번 더 계산할 것** — 안 그러면 최후의 보루로 붙은
  각주의 근거 문서가 `retrieved_context`에 실리지 않는다.
- **겹쳐 그려진 PDF 텍스트의 배수는 2배만이 아니다.** `repair_doubled_glyphs`가
  2배 전용이라 3배 런("퇴퇴퇴직직직")을 **감지조차 못 했다** — 쌍을 찾으면
  (퇴퇴)(직직) 두 쌍뿐이라 '4쌍 연속' 문턱에 닿지 않는다. 복원기가 배선돼 있는데도
  아무 일도 하지 않고 통과시켰고, doc20·doc55가 "는는는 연연연 퇴퇴퇴"인 채로
  화면에 나갔다(2026-09-01). 지금은 배수 k를 3·2로 각각 판정한다.
  또한 **판정 문자 집합에 숫자·영문·구두점을 넣지 말 것** — "계좌번호
  111222333444"가 "1234"로, 표 구분선이 짧아지는 오탐이 실측됐다. 판정은 한글
  반복만 보고, 오염이 확인된 구간에서만 숫자·구두점을 함께 걷어낸다.
  진단은 `python -m scripts.corpus_health` — **문턱 미달로 지나간 구간까지**
  보고하므로 문턱을 근거 있게 조정할 수 있다.
- **판독 실패 앵커가 이웃 손상을 물면 대조가 항상 실패한다.** 실측(2026-09-04,
  실물 158문서): 판독 실패 5,279건이 깨진 페이지 198건에 몰려 있어 **페이지당
  평균 26.7건**이다. `_repair_text`가 앞뒤 ANCHOR_LEN(12자)을 고정폭으로 그냥
  잘라내던 시절에는, 이 밀도에서 앵커 안에 **다른** 손상 구간이 함께 잘려
  들어오는 일이 흔했다. 그러면 앵커 문자열 자체에 '?'나 반복 음절이 섞이는데,
  그 오염은 이 페이지에만 있는 것이라 haystack 어디에도 글자 그대로 존재할 수
  없다 — `find()`가 항상 실패한다. 서버 실측에서 대조실패(no_match)가 전체
  실패의 96%(4,968/5,198)였는데, 합성 재현(밀집도 재현)에서 그중 다수가
  "코퍼스에 그 구절이 없어서"가 아니라 **"검색어 자체가 이미 깨져 있어서"**
  였다(고정폭 앵커 4건 중 3건이 이웃 손상을 물었다). 지금은 앵커를 **이웃 손상
  구간을 넘지 않는 만큼만** 가져온다 — 간격이 MIN_ANCHOR보다 좁으면 억지로
  채우지 않고 정직하게 앵커부족으로 분류한다. `test_이웃_손상이_앵커를_오염시키지_않는다`가
  이 시나리오를 고정한다.
- **재OCR을 시도했으나 품질을 신뢰할 수 없어 껐다 (2026-09-04~05).** 앵커부족
  3,872건(전체 실패의 73.3%)은 교차 문서 대조로 못 푼다는 실측 뒤, 원본
  이미지를 다시 읽는 `reocr.py`를 만들었다. 첫 전제(`zip_parser.py`의
  "페이지별 JPEG + OCR 텍스트 zip")는 **실물을 받기 전에 세운 가정**이었고
  틀렸다 — 실물 코퍼스는 zip이 아니라 **PDF 156건 + xlsx 2건**이었다
  (`/health`의 `ocr_repair.reocr.pages_targeted: 0`으로 드러남).
  `scripts/reocr_probe.py`로 PDF에서 이미지를 꺼낼 수 있는지 실측했더니
  **꺼내지는 것 자체는 됐지만 결과가 위험했다**:
  · doc33 — 서로 다른 두 페이지(ISA 해지 안내 / Q1~Q6 목차)의 재OCR
    결과가 글자 하나 안 틀리고 동일했다. 이미지 크기도 두 페이지가
    똑같아서(79,461B·110,845B·133,695B), "페이지에서 가장 큰 임베디드
    이미지 = 그 페이지의 스캔본"이라는 가정이 이 코퍼스에서는 안 맞았다
    — 여러 페이지가 공유하는 배경/서식 그래픽을 잘못 골랐다.
  · doc26 — 반복 문자는 없지만("SASS ATMO", "089 = wycooas oye sane
    Ba" 같은) **그럴듯해 보이는 의미 없는 영문 잡음**을 내놓았다.
    `looks_garbled`(반복 문자 판정)로는 이런 실패를 걸러내지 못한다 —
    반복이 아니라 완전히 다른 종류의 오류다.
  둘 다 `reocr.py`의 채택 기준(여전히 손상 패턴이거나 원문의 20% 미만이면
  버림)을 **통과**한다. `(판독불가)`로 정직하게 격리하는 것보다, 실재하지
  않는 문장을 근거처럼 내보내는 쪽이 "오탐이 미탐보다 나쁘다"를 정면으로
  어긴다 — 마감 전에 이 실패 양상을 걸러낼 결정론적 기준을 새로 검증할
  시간이 없어 `build_index.ingest()`에서 **호출 자체를 껐다**
  (`store.ocr_repair.reocr`는 `{"status": "비활성"}`으로 그 사실을 그대로
  드러낸다). `reocr.py`·`scripts/reocr_probe.py`·단위 테스트는 남겨
  뒀다 — 코드가 틀린 게 아니라(이미지 추출·채택 로직은 의도대로 동작한다)
  **이 코퍼스의 PDF 구조에서 "가장 큰 이미지"가 페이지 내용을 보장하지
  않는다는 것**이 문제이므로, 이미지 선택 기준을 다시 설계하고 실측으로
  검증할 시간이 생기면 재개할 수 있다.
  ⚠️ **가능하다는 것과 안전하다는 것은 다른 질문이다.** "이미지를 꺼낼 수
  있다"까지 확인하고 바로 파이프라인에 채택을 켜 두면, 검증 안 된 잡음이
  실제 서비스로 나간다. 추측 대신 실측을 요구한 덕분에 이 사고가
  배포 전에 걸렸다 — 진단(`reocr_probe.py`)과 채택(파이프라인 배선)을
  분리해 둔 것이 그 안전장치였다.
- **재OCR 후속 실측 — doc33은 고쳤지만 doc26류가 예외가 아니라 전면적이었다
  (2026-09-05).** 임베디드 이미지 추측(`page.images` + "가장 큰 것") 대신
  `pdftoppm`으로 **페이지를 통째로 렌더링**하도록 `raster_pdf_page()`를
  새로 짰다 — 어떤 이미지 객체가 그 페이지인지 고를 필요 자체를 없앤
  것이다. 재실측 결과 doc33은 정확히 고쳐졌다(서로 다른 두 페이지가 이제
  서로 다른 결과를 낸다). 그런데 **8문서·12페이지 표본 전부**에서 결과가
  "OOO"·"0000"·"INO OO OU"·"IRP - QOO0U0" 같은 라틴 문자·숫자 잡음으로
  뒤덮였다 — doc26에서 봤던 게 예외적 실패가 아니라 **이 코퍼스 스캔
  품질에서 tesseract(kor+eng)가 내는 기본 결과**였다. 이 잡음은
  `looks_garbled`를 전부 통과한다(판정 문자 집합이 '?'·한글로만 한정돼
  있어 라틴 O·0 반복은 원래 계좌번호 오탐을 피하려 일부러 빼 뒀다 —
  "겹쳐 그려진 PDF 텍스트" 항목과 같은 트레이드오프인데, 여기서는 그
  사각지대가 주된 실패 경로가 됐다).
  **결론: 이미지 선택은 고쳤지만 인식 정확도 자체가 이 스캔 품질에서
  안 나온다.**
  ⚠️ **두 번째 실측(2026-09-05, 같은 날)으로 확정했다.** 의심되는 두
  원인 — apt의 tesseract-ocr-kor가 속도 우선(fast) 모델이라는 것,
  렌더 해상도(200dpi)가 낮다는 것 — 을 동시에 고쳤다: 공식 고정밀
  (`tessdata_best`) 한국어 모델을 받아 쓰고, `RENDER_DPI`를 200→300으로
  올렸다. 같은 8문서·12페이지로 재실측한 결과 **여전히 대부분 잡음이었다**
  ("[00000 UO000", "1000 )[|"). 다만 한 가지 신호가 있었다 — doc33 p1의
  "2024.05"·"MIRAE ASSET"처럼 **크고 성긴 글자는 정확히 읽혔다.** 밀도
  높은 한글 본문만 못 읽는다는 것은, `pdftoppm`이 300dpi로 그리라고
  해도 **PDF에 박힌 원본 스캔 이미지 자체의 해상도가 낮으면 그 이상의
  디테일이 나올 수 없다**(렌더 해상도를 올리는 것은 보간이지 복원이
  아니다)는 뜻이다. 모델 품질도 렌더 해상도도 원인이 아니었다 — **원본
  스캔 해상도**가 병목이고, 이건 우리 쪽에서 손댈 수 있는 지점이 아니다
  (더 고해상도 원본을 재수집할 권한·수단이 없다). 두 가지 독립적인
  개선(모델·해상도)이 모두 실패했으므로, 이번 대회에서는 재OCR을
  **완전히 접는다** — `build_index.ingest()`는 계속 호출하지 않는다.
  `reocr.py`(페이지 렌더링 구조는 올바르게 고쳐졌다)와
  `scripts/reocr_probe.py`는 원본 고해상도 스캔을 구할 수 있게 되면
  다시 쓸 수 있도록 남겨 둔다.
- **복원 뒤 게이트를 다시 닫으면, 이미 확인한 오염을 놓친다 (2026-09-05
  실측).** 서버 집계에서 `runs_masked`(5,189)가 `runs_found - runs_repaired`
  (5,256)보다 67건 적었다 — "복원도 안 됐는데 격리도 안 된" 손상이
  있다는 뜻이었다(이 절의 numbers-don't-add-up 질문에서 재현). 원인:
  한 페이지에 STRONG(3+) 런과 WEAK(2글자) 런이 함께 있을 때, STRONG 런만
  복원되고 나면 남은 텍스트에는 3+ 패턴이 더 이상 없다. `mask_unreadable`
  (공개 진입점)은 `looks_garbled`(3+ 기준)로 **다시** 게이팅하므로, 이미
  `repair_documents`가 `garbled` 목록에 넣을 때 STRONG 판정을 거친
  페이지인데도 마스킹을 통째로 건너뛰어 미복원 2글자 런이 원문 그대로
  샌다 — 이 모듈 전체가 막으려던 "?????????가 그대로 청크에 실린다"와
  정확히 같은 사고다. 지금은 `repair_documents`가 게이팅 없는
  `_mask_weak`를 직접 쓴다(이미 STRONG 판정을 거친 페이지만 다루므로
  재게이팅이 불필요하다). `mask_unreadable`(다른 호출자, 예컨대
  `store.py`의 적재 시점 안전망)의 게이팅 동작은 그대로 유지했다 — 그
  자리는 STRONG 판정을 사전에 거치지 않은 임의 텍스트를 받으므로 게이트가
  필요하다. **집계 항목끼리 산수가 맞지 않으면 반올림 오차로 넘기지 말고
  실제로 새는 자리가 있는지 봐야 한다** — `runs_found = runs_repaired +
  runs_ambiguous + runs_no_match + runs_weak_anchor`는 항상 성립해야
  하고, `runs_masked`도 정확히 그 미복원분과 같아야 한다.
  `test_강한_런이_복원되고_약한_런만_남아도_마스킹된다`가 이 시나리오를
  고정한다.
- **⚠️ 걷어낸 조기 거절 규칙이 다른 계층에 남아 있었다 (2026-09-05, 안내서
  예시 질의로 발견).** L0에서 "도메인 커버리지·근거 0건 조기 REFUSE"를
  없앴는데, **같은 규칙이 `decide_answerability`에 그대로 살아 있었다.**
  경로 분류는 ADVISORY로 옳게 갔지만 이 게이트가 L4-sub에 닿기 **전에**
  잘라내, 과제 안내서 4페이지의 주최 측 예시 질의
  "58세인데, 크게 잃지 않으면서 굴릴 상품 하나 추천해 주세요."가
  **"제공된 자료 범위에서는 답변드리기 어렵습니다"로 거절됐다.**
  거의 같은 "좋은 연금 상품 하나 추천해 주세요"는 근거가 잡혀 정상
  응답했으므로 — **정보를 더 준 질의가 거절되는** 상태였다.
  어긴 불변식이 셋이다: 핵심 설계 원칙 "불특정 서술도 답한다",
  아키텍처 "L4-sub … 되돌려보내지 않는다", 그리고 평가지표
  **'정보한계 대응'**(무리한 답변 대신 한계 고지 **또는 역질문** — 맨몸
  거절은 둘 다 아니다). 지금은 `is_advisory=True`면 REFUSE 대신
  ASK_BACK이다. **GENERAL은 그대로 거절한다** — 계산·비교를 제도적 근거
  없이 답하면 그게 날조다. ADVISORY만 예외인 이유는 그 답변이 사실을
  단정하지 않고 확인할 것을 정리해 주기 때문이다(바로 아래 NO_SLOTS를
  ADVISORY에서 제외하는 것과 같은 논리).
  ⚠️ **한 계층에서 규칙을 걷어냈으면 같은 규칙이 다른 계층에 없는지
  확인할 것.** 부품만 고치고 호출자가 인자를 안 넘기면 그대로 거절되므로
  배선 테스트(`test_안내서_예시질의가_거절되지_않고_역질문을_받는다`)를
  함께 둔다.
- **한국어 부분문자열 매칭.** "불가능합니다"는 "가능합니다"를 포함하고,
  "정해지는"은 "해지"를 포함한다. 규칙에 단어를 넣을 때는 경계를 준 정규식을 쓸 것.
- **Sub-Agent의 두 역할을 한 프롬프트로 합치지 말 것.** 진단
  (`SUB_AGENT_SYSTEM_PROMPT`)은 "답변을 작성하지 마십시오"가 핵심이고, 구제 생성
  (`SUB_AGENT_REWRITE_PROMPT`)은 답변을 써야 한다. 합치면 정상 흐름에서 진단만
  해야 할 계층이 답변을 쓰기 시작한다.
- **구제 답변의 재검증을 빼지 말 것.** 성능을 위해 `verify_grounding` 재호출을
  생략하고 싶어지는 자리인데, 그 순간 Sub-Agent가 감사를 우회하는 뒷문이 된다.
- **`llm_call_adapter`의 기본 `max_tokens`는 800이다.** 감사용 JSON 판정에 맞춘
  값이라 답변 본문 생성에 그대로 쓰면 **문장이 중간에 잘린다.** 생성 용도로 쓸
  때는 `purpose`와 `max_tokens`를 함께 올릴 것.
- **HCX가 마크다운을 쓸 수 있다는 것을 잊지 말 것.** 프롬프트에 금지를 명시해도
  (SUPERVISOR_SYSTEM_PROMPT·ADVISORY_SYSTEM_PROMPT·SUB_AGENT_REWRITE_PROMPT
  전부) 관성적으로 `**굵게**`를 쓸 수 있다. 실연동에서 실제로 확인됐다
  (2026-09-01) — "600만 원"이 "**600만 원**"으로 사용자 화면에 그대로
  나갔다. mock 300건 스캔에서는 0건이었다 — mock은 템플릿 렌더러라 애초에
  마크다운을 안 쓰기 때문에 이 결함은 **실연동에서만 보인다.** 프롬프트
  지시만으로는 부족하므로 `strip_markdown()`으로 생성 직후 결정론적으로
  제거한다. **반드시 `verify_grounding`보다 앞에서** 적용할 것 — 검증이
  본 텍스트와 사용자에게 나가는 텍스트가 다르면 그 자체가 사고다.
- **제공 OCR 텍스트의 '?'는 우리가 만든 것이 아니다.** 우리 디코더의 대체문자는
  '�'이지 '?'가 아니다(`zip_parser._decode`·`loader._decode`). 즉 판독기를 바꿔서
  고칠 수 있는 결함이 아니다. 실물에서 확인됐다(2026-09-01) — "납입금액부터
  ?????????"가 청크를 타고 사용자 화면에 그대로 나갔다. **mock에서는 0건이라
  실물에서만 보인다** (마크다운 노출과 같은 계열). 복원 수단은 재OCR이 아니라
  **코퍼스의 중복**이다 — 투자설명서들이 같은 [연금저축계좌 과세 주요 사항] 표를
  거의 그대로 싣기 때문에, 깨진 자리의 앞뒤를 앵커로 다른 문서에서 같은 구절을
  찾아 메울 수 있다(`ocr_repair.py`). 지어내는 것이 아니라 코퍼스 안에 실재하는
  텍스트를 가져오는 것이므로 근거가 있고 결정론적이다. 지킬 것 셋:
  ① **haystack은 복원 전 원문으로 한 번만 만든다** — 고쳐가며 대조하면 한 번의
  오복원이 다음 복원의 근거가 돼 코퍼스 전체로 번진다.
  ② **후보가 갈리면 복원하지 않는다.** 억지로 하나를 고르면 그 순간 날조다.
  ③ **적재 시점 안전망(`ChunkRecord.from_dict`의 `mask_unreadable`)을 빼지 말 것.**
  인덱스는 호스트 볼륨에 남고 entrypoint가 재사용하므로, 이미지를 새로 빌드해도
  예전 인덱스에 박힌 '?'는 사라지지 않는다. 복원까지 받으려면 **재인덱싱이 필요**하다
  (`FORCE_REINDEX=true` 또는 `docker compose run --rm reindex`).
- **OCR 판독 실패의 대체문자는 '?'만이 아니다.** doc55에서는 인식 실패 자리마다
  '?'가 아니라 **하나의 한글 음절('퇴')을 반복해서 채우는 형태**로 나타났다
  (2026-09-02 실물 확인) — "퇴퇴 퇴 퇴퇴퇴퇴 : IRP 퇴퇴퇴퇴 퇴퇴 퇴퇴(…)". 숫자·영문은
  멀쩡한데 한글만 통째로 한 음절로 무너진다 — 도메인에서 흔한 음절로 잘못 수렴하는
  것은 이미지 기반 OCR이 확신을 잃었을 때 흔한 실패 양상(반복 붕괴)이다. 근본 원인은
  '?'와 같으므로(판독기가 못 읽은 자리를 대체문자로 채움) `ocr_repair.py`의 판정을
  **특정 문자에서 "같은 글자가 3회 이상 반복"으로 일반화**했다('?' 또는 한글 한
  음절 — 숫자·영문·구두점은 여전히 제외, `loader.py` 겹침 복원기가 이미 겪은 오탐
  계열이기 때문). 복원·격리 메커니즘은 그대로다. mock 코퍼스 전수 + 298건 질의
  전수 오탐 0건 확인(단, 사용자가 강조로 "??????"를 쓴 질의 1건은 예외 — 이 탐지기가
  코퍼스 문서에만 적용되고 사용자 질의에는 적용되지 않아 무관하다).
- **인용 기준과 매칭 기준을 어긋나게 두지 말 것.** 같은 판단(이 근거가 이 슬롯을
  뒷받침하는가)을 두 곳이 다른 기준으로 하고 있었다 — `slot_matching._overlap_ok`는
  **도메인 핵심어 겹침**을 요구하는데, `answer_prompt._evidence_snippet`은 아무
  핵심어나 **부분문자열**로 1개만 있으면 통과였다. 느슨한 쪽이 답변 본문에 직접
  들어가므로, **매칭이 거부한 근거가 인용문으로 새어 나갔다** — "연간 2천만원
  수령 시 세금 범위" 슬롯에 금융소득종합과세(이자·배당) 조항이 붙었다.
  '연간'·'천만원'만 겹쳤을 뿐 연금과 무관한 문단이다.
  또한 상위 1건만 보고 판단하면 **한 문서 안에서 엉뚱한 청크가 앞설 때 맞는
  근거까지 버린다** — 도메인 겹침이 있는 청크만 남기고 그 안에서 골라야 한다.
- **표시 반올림과 수치 검증의 기준을 어긋나게 두지 말 것.** `format_manwon`은
  만원 단위에서 정수로 반올림한다(76.56 → "77만원"). 검증기 대조 집합에 원본
  값만 있으면 **시스템이 스스로 표시한 값이 '근거 없는 수치'로 잡힌다**
  (77 vs 76.56 = 0.575%, 허용 오차 0.5%를 간발의 차로 초과). 그러면 L5'가 쓴
  답변이 통째로 버려지고 템플릿 불릿 나열로 축퇴돼, 사용자에게는 "형식이
  왜 이래?" 소리를 듣는 답변이 나간다. 해법은 **허용 오차를 키우는 것이
  아니라**(진짜 날조까지 통과한다) `_flatten_numbers`가 표시형도 허용 집합에
  넣는 것이다 — 계산함수 출력에서 결정론적으로 파생된 값은 정의상 근거가 있다.
- **`render_calc_result()`는 15종 계산함수 공용 렌더러다.** 새 계산함수가
  중첩 dict(선택지 비교 등 이색 구조)를 반환하면 raw 영문/언더스코어 키가
  번역 없이 그대로 노출된다. `compare_taxation_options()`가 실제로 그랬다
  ("separate:", "comprehensive:"가 사용자 화면에 그대로 찍힘). 이색 구조를
  반환하는 함수를 추가할 때는 `_LABELS`에 키를 등록하거나, 이미 있는
  `_render_tax_choice()`처럼 전용 문장 렌더러를 만들 것.
- **비금액 숫자 조건에도 범위 검증이 있어야 한다.** `calc_params.py`의 `_VALID`는
  계산 인자로 쓰일 때만 걸러 준다. `conditions.py`의 LLM 조건 병합 루프
  (`_NUMERIC_CONDITION_BOUNDS`)에 같은 경계가 없으면, 계산에는 안 들어간
  값(그 답변이 마침 그 인자를 안 쓰는 계산이었던 경우)이 검증 없이
  "조건으로 이해했습니다" 줄에 그대로 노출된다. 실측: "연간 연금수령액
  2,000만원"을 HCX가 `pension_year=2000`으로 잘못 채운 값이 사용자에게
  그대로 보였다. 두 경계표는 반드시 같은 수치를 유지할 것
  (`test_condition_bounds.py`의 대조 테스트가 어긋나면 실패한다).

## 도메인 함정 — 코드에 반영해야 할 것

`trap_rules.py`에 28종이 정리돼 있으나, 특히 다음은 로직에 직접 반영됨:

- **연금수령연차 ≠ 연금실제수령연차** — 전자는 수령한도, 후자는 퇴직소득세 감면율 결정
- **1,500만원 초과 시 전액이 과세 대상** (초과분만이 아님)
- **연금 외 수령은 재원마다 세금이 다르다** — 투자설명서가 "기타소득 분리과세
  16.5%" 뒤에 **"단, 이연퇴직소득은 퇴직소득 과세기준 적용"**을 달고 있다.
  퇴직급여 이체분은 퇴직소득 과세기준, 세액공제분·운용수익은 기타소득세 16.5%,
  세액공제 안 받은 납입액은 과세 제외다. "해지하면 전액 16.5%"는 오답이다 (A9)
- **15% vs 16.5%는 지방소득세 포함 여부 차이** (상충 아님, 15 × 1.1 = 16.5)
- **구법 수치 문서 혼재** — 700만원/1200만원/4천만원은 개정 전 수치
- **판매 클래스 가입자격** — 총보수 최저 클래스가 가입 불가인 경우가 많음

## 상품 팩트 — 6축 (2026-09-04 신설)

과제 안내가 정의한 실적배당형 상품 데이터의 축은 여섯이다.
**두 곳에서 나눠 뽑되, 답변에는 한 벌로 실린다.**

| 축 | 추출 위치 | 시점 |
|---|---|---|
| 판매클래스 · 총보수 | `analysis/products.py` | 검색 후 (근거 청크) |
| 상품분류 · 위험등급 · 수익률 · 시장잔고 | `analysis/product_facts.py` | **색인 시점 (문서 전문)** |
| 확정금리 (원리금보장형) | `analysis/product_facts.py` | 색인 시점 · 6축 밖 신설 |

- **수익률은 값으로 확정하지 않고 표를 원문 인용한다.** 과제 안내가 6축에
  넣었으므로 빼지 않되, 실물은 헤더와 값이 다른 줄에 있는 **다단 표**이고
  OCR이 깨져 있어(`3.554 | 080 | 0.0303`) 컬럼을 정렬해 파싱하면 **엉뚱한
  펀드에 엉뚱한 수익률**이 붙는다 — 누락보다 나쁜 실패다. 그래서
  `extract_return_table()`이 표를 잘라 **그대로** 싣고, 한 줄에 기간과
  수치가 함께 있는 명시적 문장(`최근 1년 수익률 3.87%`)만 값으로 확정한다.
  프롬프트가 "과거 실적이며 미래를 보장하지 않는다 · 예상/기대 수익률로
  쓰지 말 것 · 표에서 임의로 값을 골라 단정하지 말 것"을 명시하고,
  준법 감사의 `_ASSERTIVE_PATTERNS`가 '예상·전망·기대 수익률' 표현을 막는다.
- **확정금리는 수익률과 다른 축이다.** 과제 안내 5페이지가 상품을
  원리금보장형(예금·GIC)과 실적배당형(펀드·ETF)으로 나누는데, 6축은
  실적배당형 전제다. 원리금보장형의 **약정이율은 계약상 확정된 값**이라
  결정론적으로 뽑아도 왜곡이 없다. 섞으면 "보장된 이율"과 "지나간 실적"이
  답변에서 같은 이름으로 나가므로 축을 분리했다.
  ⚠️ **연체이율·중도해지이율을 거부한다** — 실재하는 이율이지만 그 상품의
  약정이율이 아니다. 중도해지이율을 "이 상품 금리"로 답하면 오답이다.
  ⚠️ **실측 0건이지만 패턴 실패가 아니라 데이터 부재다.** '약정이율'은
  25회·17문서에 있으나 전부 위험 고지문("중도해지 시 약정이율의 축소 적용
  등 불이익")이고 수치가 없다. 차단어가 정확히 그것을 막았다 — 안 막았다면
  엉뚱한 값이 그 상품의 금리로 나갔다. 이 코퍼스에는 원리금보장형 상품
  문서가 없다(공시이율·적용금리 등장 0회). 패턴은 그대로 둔다.

- **커버리지 실측 (158문서, 2026-09-04)** — 축을 넓힐 때의 기준선이다.

  | 축 | 커버리지 | 방식 |
  |---|---|---|
  | 위험등급 | 63.3% | 값 확정 (자기분류 패턴 96회) |
  | 수익률 | 63.3% | 표 원문 인용 |
  | 총보수표 | 63.3% | 표 원문 인용 (기존 값 추출 3.2%를 보완) |
  | 상품분류 | 32.9% | 값 확정 |
  | 시장잔고 | 8.2% | 값 확정 (규제 기준선 차단 후) |
  | 확정금리 | 0% | 데이터 부재 (위 참조) |

  투자설명서는 158건 중 약 100건이다(총보수 100·위험등급 101 기준).
  나머지는 제도 안내·Q&A 문서라 이 축들이 없는 것이 정상이다.
  **63.3%는 투자설명서 기준으로는 사실상 전수에 가깝다.**

- **왜 색인 시점인가.** `extract_class_expenses`는 **검색된 청크만** 본다.
  검색이 표 청크를 못 건지면 사실이 통째로 사라진다 — 검색 순위에 사실의
  존재 여부가 걸려 있는 셈이다. 새 추출은 `build_doc_metadata()`에서
  문서 전문을 훑어 `doc_meta["product_facts"]`에 남기므로 사라지지 않는다.
  ⚠️ **재인덱싱해야 반영된다.** 인덱스는 호스트 볼륨에 남고 entrypoint가
  재사용하므로, 이미지를 새로 빌드해도 예전 인덱스에는 팩트가 없다.
- **왜 정규식인가 (MRC/NER이 아니라).** 투자설명서는 `【제목】` + 파이프 표라는
  규칙적 형태다. "이 상품의 위험등급이 몇 등급인가"는 **판단**이지 생성이
  아니므로 "판단은 코드, 문장은 LLM"이 그대로 적용된다. span 추출 모델은
  확률적이라 틀린 구간을 높은 확신으로 반환해도 결정론적으로 기각할 수 없고,
  torch 모델이 하나 더 상주한다(현재 torch+e5-small만으로 1~1.5GB).
- **⚠️ 위험등급은 1등급이 가장 위험하다.** 숫자만 주면 "1등급이라 안전하다"는
  정반대 서술이 나온다. 그래서 값과 함께 **원문 표기**(`4등급(보통 위험)`)를
  싣고, 프롬프트 블록이 방향을 명시한다. 표준 6단계와 어긋나는 표기
  (`1등급(매우 낮은 위험)`)는 버리지 않고 `warning`으로 드러낸다 —
  구형 5단계이거나 판독 오류일 수 있고, 둘 다 "틀렸다"고 단정할 근거가 없다.
- **값이 갈리면 확정하지 않는다.** 한 문서에서 서로 다른 등급이 나오면
  비우고 `conflicts`에 사유를 남긴다(`ocr_repair`의 "후보가 갈리면 복원하지
  않는다"와 같은 규칙). 같은 값의 반복은 정상이므로 충돌이 아니다.
- **⚠️ 팩트 스니펫은 수치 검증의 허용 집합에 들어가야 한다.** 원문 스니펫을
  `fact_texts`로 넘기지 않으면, 검색이 마침 표 청크를 놓쳤다는 이유로
  **우리가 문서에서 확정한 위험등급·수익률이 '근거 없는 수치'로 잡혀**
  답변이 통째로 템플릿으로 축퇴한다. 실측: 팩트 미주입 시 `[1.0, 3.87, 4.0]`
  전부 미접지 → 주입 후 통과. 이건 F8을 무르는 것이 아니다 — F8이 막은 것은
  "검색된 근거 뭉치 전체"를 허용하는 것이었고, 팩트 스니펫은 결정론적으로
  잘라 온 코퍼스 원문이며 **근거로 채택된 문서의 것만** 들어온다.
- **근거에 오르지 않은 문서의 팩트는 쓰지 않는다.** 색인에는 158문서의 팩트가
  다 있지만, 검색이 고르지 않은 문서의 수치를 답변에 쓰면 근거 없는 인용이다.
  `collect_facts()`가 evidence의 doc_id로만 조회한다.
- **⚠️ 반대로, 답변이 쓴 팩트의 문서는 `retrieved_context`에 실려야 한다
  (2026-09-05 배선 테스트로 발견).** 팩트 블록은 `evidence`(검색 결과)로
  만들어 생성 프롬프트에 실리는데, 인용은 `_used_evidence`(**슬롯에 매핑된**
  근거)로만 만들어졌다. 두 집합은 같지 않다 — 슬롯 매핑이 하나도 성립하지
  않으면 인용이 비어 `retrieved_context`에 "근거 문서 없음"이 찍히는데,
  그 사이에도 팩트 블록은 위험등급·총보수를 생성기에 그대로 넘긴다. 결과는
  **"답변에는 4등급이 있는데 그 근거 문서가 retrieved_context 어디에도 없는"**
  상태다. 평가 API 스펙이 `retrieved_context`를 "답변 생성에 참고한 검색
  문서"로 정의하므로 스펙 위반이고, 근거 0건 표시로 실존 상품을 말하던
  사고와 같은 계열이다. 지금은 `facts_reflected_in_answer()`가 팩트 표기가
  답변에 **글자 그대로** 있는 문서를 골라 인용에 되살린다 — 함정 근거 문서를
  `_addressed_trap_docs`가 같은 이유로 되살리는 것과 정확히 같은 처리다.
  표 축의 label은 값이 아니라 자리표시자("총보수 표(원문 인용)")이므로
  반영 판정에서 제외한다. 이 판정은 **인용을 더하는 방향으로만** 쓰므로
  오탐이 강등이나 재생성을 부르지 않는다.
- **⚠️ 표 원문 인용은 다음 절 제목에서 멈춰야 한다 (2026-09-05).**
  `_quote_table`이 줄 수(`_TABLE_LINES=6`)로만 잘라, 표가 짧으면 뒤 절이
  딸려 왔다 — 총보수표 스니펫에 "【집합투자기구의 규모】 설정원본 1,234억원"이
  함께 실렸다. 이 스니펫은 ① 생성 프롬프트에 "이 값만 사용 가능"으로 실리고
  ② `fact_snippets`를 거쳐 **수치 검증 허용 집합**에 들어간다. 즉 다른 절의
  수치가 총보수인 것처럼 근거를 얻는다 — F8("근거 뭉치 전체를 허용 집합에
  넣지 않는다")과 같은 결함이다. 투자설명서는 `【제목】`으로 절이 갈리므로
  그 경계에서 자른다(머리말 자신이 `【…】`이므로 첫 줄은 제외).
- **제목과 값이 다른 줄에 있는 형태를 놓치지 말 것 (2026-09-05).**
  실물 투자설명서의 지배적 형태는 `【집합투자기구의 종류】` 다음 **줄**에
  값이 오는데, `종류_표제` 패턴이 `[^\n]`으로 줄에 묶여 있어 통째로 놓쳤다
  (같은 문서에서 위험등급 63.3% vs 상품분류 32.9%의 격차가 여기서 왔다).
  지금은 `[^【]`로 줄을 넘되 **다음 절에서 멈춘다.**
  ⚠️ **같은 완화를 `분류_표제`에 하면 안 된다** — '투자대상' 같은 표제어
  뒤에는 분류값이 아니라 산문이 온다. 넘기게 했더니
  "【투자대상】 / 이 펀드는 주식형 자산에 투자하지 **않습니다**"가
  상품분류=주식형으로 잡혔다(부정문을 정반대로 확정). 두 패턴의 비대칭은
  의도된 것이므로 "일관성"을 이유로 맞추지 말 것.
- **표기 하나가 빠지면 그 표기를 쓴 문서에서 축이 통째로 사라진다.**
  `_ASSET_CLASSES`에 '혼합채권형'(법령 어순)만 있고 '채권혼합형'(협회 분류·
  실물 문서 어순)이 없었다. `_AUM_PATTERNS`에는 표준 표기 '설정원본'이
  빠져 있었다(시장잔고는 커버리지 8.2%로 6축 중 최저라 영향이 크다).
  둘 다 순수 가산이고 오탐 기전은 기존 항목과 같다(`_pick_one`이 값 충돌 시
  비우고, `_AUM_BLOCKLIST`가 규제 기준선을 막는다).
  ⚠️ **실물 발화 횟수는 서버에서 `python -m scripts.corpus_facts`로 확인할 것**
  — mock 코퍼스에는 이 축들이 0회라 로컬에서는 측정 자체가 불가능하다.
- **두 경로에 같은 블록을 싣는다.** L5'(`build_supervisor_payload`)와
  L4-sub(`build_advisory_payload`) 둘 다 `render_facts_block()`을 부른다.
  ADVISORY에만 빠지면 "상품 하나 추천해 주세요"에서 정작 위험등급·보수를
  말하지 못한다 — F3(함정 교정이 ADVISORY에만 빠져 있던 것)과 같은 계열이다.
- **패턴은 실측으로 추린다.** `python -m scripts.corpus_facts` 가 축별 커버리지,
  **패턴별 발화 횟수**, 그리고 **키워드는 있는데 값이 안 뽑힌 줄**(near-miss)을
  함께 보고한다. 이게 없으면 "패턴이 맞아서 0건"인지 "틀려서 0건"인지 구별할 수
  없다. mock 코퍼스에는 이 네 축이 **등장 횟수 0회**라 로컬 검증이 불가능하다 —
  실물에서만 보이는 계열이므로 반드시 서버에서 돌려 볼 것.

## 금액 단위

계산함수 전체가 **만원 단위**다. 사용자 질의는 원 단위인 경우가 많으므로
입출력 경계에서 변환하고 내부는 만원으로 통일할 것. 혼동 시 1만배 오차가 난다.

경계는 `calc_params.py`의 `_VALID` 상한이 지킨다. 각 인자에 제도상 상한
(연금저축·IRP 개인납입은 연 1,800만원 등)을 두어, 원 단위 값이 흘러들어오면
계산되지 않고 **되묻는 항목으로 전환된다.** 상한을 None으로 되돌리면
1만배 오차가 조용히 통과한다.

## 코드 스타일

- 한국어 주석. 도메인 용어는 한국어 그대로 (연금수령한도, 세액공제 등)
- 계산 함수는 순수함수. 반환값 dict에 `source` 필드로 근거 문서 ID 포함
- 예외를 삼키지 말 것. 특히 감사·검증 실패는 반드시 think_trace에 기록
- 새 기능보다 미구현 연결이 우선. 마감이 촉박하다

## 테스트

- 계산함수는 회귀 테스트로 기대값 고정 (제공문서 원문 예시 재현 확인)
- 함정 규칙 추가 시 참고 질의 5개로 오탐 회귀 확인
  (과거 "정해지는"의 '해지'가 규칙에 걸리는 오탐 발생 이력 있음)
- **판정 규칙을 새로 넣을 때는 실측부터 한다.** 단위 테스트만으로는 오탐률을
  알 수 없다. `scripts/replay_audit_300.py`의 질의 298건을 실제 파이프라인에
  통과시켜 발화율을 세어 볼 것. 정상 답변에서 발화하면 그 규칙은 틀린 것이다.
- 의도적으로 동작을 바꿀 때, 깨진 테스트를 약화시키지 말 것.
  **새 불변식을 주장하도록 고쳐 쓰고, 왜 바뀌었는지 테스트에 적는다.**

## 데이터 취급

- `data/corpus/` 는 대회 제공 문서다. **gitignore 유지 필수** — 커밋되면 안 된다.
- `data/law/` 는 법제처 수집본이다. 배포 시 수집하며 저장소에 넣지 않는다.
