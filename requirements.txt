# 연금 Agent 런타임 의존성
# ─────────────────────────────────────────────────────────────
# 원칙: 답변 문장을 만드는 호출(L1·L5'/L4-sub·L6·Sub-Agent)은 HyperCLOVA X
#       전용이다(대회 제약 1). 임베딩 모델은 검색 순위용 벡터일 뿐 답변
#       문장을 만들지 않으므로 이 제약 밖이고, 타사 모델 사용이 확인됐다
#       (2026-08-19). 그래서 임베딩/NLI 라이브러리는 여기(런타임 필수)가
#       아니라 선택 설치인 requirements-embedding.txt로 따로 뺐다.
#       BM25는 순수 파이썬 구현(app/retrieval/bm25.py)이라 별도 의존성이 없다
#       — 임베딩 없이도 서비스는 완전히 동작한다.

fastapi==0.141.1
uvicorn[standard]==0.32.1
pydantic==2.12.4
httpx==0.28.1

# 선택 의존성 — PostgreSQL(pgvector/tsvector) 백엔드를 쓸 때만 필요.
# 현재 기본 경로는 파일 기반 인덱스(app/ingest/store.py)이므로 없어도 동작한다.
# 자세한 내용은 sql/schema.sql 및 README의 "검색 백엔드" 절 참고.
psycopg[binary]==3.2.3
pgvector==0.3.6

# 선택 의존성 — 문서 형식별 판독기.
# 없어도 서비스는 뜬다(해당 형식만 '판독 실패'로 보고된다).
# 제공 자료에 PDF/엑셀이 섞여 있으면 설치할 것.
pypdf==5.1.0
openpyxl==3.1.5

# 선택 의존성 — 판독 실패 페이지 재OCR(app/ingest/reocr.py)에 쓴다.
# 시스템 tesseract-ocr(+ tesseract-ocr-kor) 바이너리가 별도로 필요하다
# (Dockerfile에 이미 설치돼 있다). 바이너리가 없으면 재OCR만 건너뛴다.
pytesseract==0.3.13
Pillow==10.4.0

# 테스트
pytest==8.3.4
