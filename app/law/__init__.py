"""법령 접지 계층.

외부 법령 조문을 수집해 두고, HCX가 내린 함정 판정의 근거가 그 조문에
**글자 그대로 실재하는지** 대조한다. 대조에 실패한 판정은 폐기된다.

구성
----
  schema.py         조문 구조체 + 인용 대조 원시 연산
  store.py          수집본 적재·조회 (읽기 전용)
  crawler.py        법제처 OPEN API 수집기 (오프라인 실행)
  citation_guard.py 할루시네이션 차단선 — 판정 검증과 반영
  anchors.py        함정 규칙 → 근거 조문 앵커 (검증된 것만 등재)

⚠️ 법령 조문은 **내부 검증 전용**이다. retrieved_context에는 제공문서만
   싣는다. 평가 규정이 '제공 문서 근거'를 요구할 때 안전한 쪽을 택한 것이다.
"""
