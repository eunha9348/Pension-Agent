# 진행 보고서

작성: 2026-08-15 (야간 자동 작업) · 최종 갱신 2026-08-19

> ⚠️ **이 문서는 2026-08-19 시점의 개발 일지입니다.** 아래 내용 중
> "CLOVA 실연동 확인 못 함"·"실물 문서 없음"·"테스트 337건"·
> "18/42문항" 등은 그 시점 스냅샷이며 **이후 전부 바뀌었습니다.**
> 현재 상태(실물 158문서 인덱싱 완료, CLOVA 실연동 확인 완료, 테스트
> 1,250건 통과, L0~L6 전 계층 + 상품 팩트 6축 + 법령 계층 구현 완료
> 등)는 **[README.md](README.md)와 [CLAUDE.md](CLAUDE.md)를 우선
> 참고하십시오.** 이 파일은 08-15~08-19 개발 초기 궤적을 남긴 기록이지
> 최신 상태 보고서가 아닙니다. 그 이후의 결함 발견·수정 이력은
> CLAUDE.md의 "되돌리기 쉬운 함정" 절에서 계속 이어집니다.

브랜치: `claude/claude-implementation-plan-hpz25z`
테스트(당시 시점): **337건 통과 + 3건 건너뜀** · 자체 평가셋 **42문항**

---

## 0-AC. 실배포 오답 2건에서 출발한 전면 개선 (2026-08-19)

실배포 질의 2건(Q-001 명퇴 교사 / Q-002 솔로몬 국공채)이 모두 핵심에서
틀렸다. 원인을 코드까지 추적한 결과 **두 실패의 뿌리가 하나**였다 —
검색이 정답 문서를 가져오지 못했다. 로드맵 보고서의 A~E 순서대로 전부 반영했다.

### A. 검색 정확도

| # | 무엇이 문제였나 | 어떻게 고쳤나 |
|---|---|---|
| A-1 | 함정 규칙이 `source`에 근거 문서를 갖고 있고 L2가 L3보다 먼저 도는데, **그 정보가 검색에 전달되는 경로가 없었다** | 감지된 함정의 문서를 별도 검색 패스로 훑어 최대 3슬롯 예약. 질의로는 사용자 원문이 아니라 함정의 `fact` 문장을 써서 구어체↔법령체 격차를 넘김 |
| A-2 | 중복 제거 로직이 **아예 없었다**. Q-001 근거 8건 중 6건이 동일 보일러플레이트 | 토큰집합 Jaccard ≥0.85 근중복 제거 |
| A-3 | 한 문서가 근거를 독점 | 문서당 최대 2청크, 후보 부족 시 완화 |
| A-4 | 펀드명이 반복되는 연혁 청크가 본문을 밀어냄 (Q-002 환각의 직접 원인) | 연혁·목차형 청크 점수 강등. **제거가 아니라 강등** — "언제 신설됐나" 질의에는 연혁이 정답이므로 |
| A-5 | 임베딩 미사용(제약 미확인) | 주최측 **허용 확인** → CLOVA Studio 임베딩(bge-m3) 연동. 건당 1회 호출이라 별도 단계로 분리, 본문 해시 증분, float32 이진 저장(numpy 불필요), 실패 시 BM25 축퇴 |

### B. 검증 신뢰성 — 가장 위험했던 부분

`supervisory_board.py`의 함정 감사가 **단어 존재 검사**였다. 답변에 "주의"
두 글자만 있으면 감지된 함정 **전부**가 해소 처리됐다. Q-002에서
"유동성 관리에는 주의가 필요합니다"가 전혀 무관한 D2를 통과시켰다.

오답은 한 문항을 잃지만 이건 **차별점 전체**를 잃는다 — 심사위원이 이 6줄을
보면 리포트의 모든 "감사 통과"가 동시에 의심받는다.

- **B-1** 26개 규칙 각각에 검증 핵심어를 부여하고 규칙 단위로 판정. critical
  미해소는 REVISE, 그 외는 DOWNGRADE. 시정 지시도 구체화(뭉뚱그린 지시라
  재생성도 실패했었다)
- **B-2** `_TRIVIAL_MAX=12`가 "만기 3년·7년"을 검증 대상에서 통째로 제외했다.
  단위(년/개월/세/등급/%)가 붙으면 작아도 검사. 조문번호·목록순서는 계속 제외
- **B-3** 대조 대상 0건인데 "통과"로 표기하던 것을 "대조할 수치 없음(통과 아님)"으로

### C. 정직한 축퇴 — 최고 가중치 지표

Q-001에서 L6는 초안과 재생성을 **모두 반려**했다. 시스템이 자기 답변의
부적절함을 두 번 확인하고도, 사용자에게는 아무 표시 없이 확신에 찬 어조로
답했다. 트레이스에는 "보수적으로 원본 유지"라고 적혀 있었지만, 반려된 답변을
그대로 내보내는 건 보수적인 처리가 아니다.

- **C-1** 재생성 기각·생략 시 검증 미통과 사실을 답변에 명시하고 확인 항목으로 전환
- **C-2** 미해소 REVISE는 ANSWER→PARTIAL 강등. PARTIAL 이하는 이미 한계를
  고지하는 등급이라 더 낮추지 않음(과잉 축퇴 방지)

### D. 답변 품질

Q-001의 진짜 오류는 L1에 있었다. "세금감면"이라는 표현에 이끌려 의도를
'세액공제'로 잡았는데, 명퇴수당은 이미 발생한 퇴직소득이라 맞는 제도는
**이연퇴직소득세 감면**이다. 더 나빴던 건 구조 — LLM 슬롯이 규칙 슬롯을
통째로 대체해서, **화면에 표시된 계획과 실제 실행된 슬롯이 달랐다.**

- **D-1** `reconcile_spec()` — 퇴직소득 신호가 있으면 LLM의 세액공제 분류를
  뒤집는다. 슬롯은 대체가 아니라 병합. `planned_calls`를 최종 슬롯에서 다시
  만들어 계획과 실행을 일치시킴
- **D-2** [주의할 혼동]에 항목별로 "이 용어가 반드시 답변에 등장해야 함"까지
  명시. 수치 **관계** 정확성도 요구(600/900을 "선택"으로 오독한 사례)

### E. 평가 인프라

18 → **42문항**. 함정 26종 각각 최소 1문항, 실배포 오답 2건을 L-01·L-02로 고정.
**`must_cite` 추가** — 답변 문장만 채점하면 "그럴듯한데 엉뚱한 문서를 근거로
든" 경우를 놓친다(Q-001이 정확히 그랬다). 검색 개선 효과는 이 항목으로만 측정된다.

평가셋을 만들다 결함 2건을 추가로 발견해 고쳤다:

- `_used_evidence()`가 슬롯 매핑이 없으면 상위 2건을 '검색 근거'로 붙였다.
  그래서 "부동산 투자 조언" 질의에 판매 클래스 가입자격 문서가 인용됐다.
  → 인용하지 않고 "근거 문서 없음"을 명시. **무관한 근거가 있는 척하는 것이
  근거가 없는 것보다 나쁘다**
- 평가 하네스의 거절 감지 문구가 실제 출력과 달라, 제대로 거절한 것을
  실패로 표시하고 있었다

### ⚠️ 배포 서버에서 반드시 할 일

로컬은 mock 코퍼스라 3문항을 건너뛴다. 실물 158문서에서 확인해야 한다.

```bash
git pull origin claude/claude-implementation-plan-hpz25z
docker compose --profile tools run --rm embed    # 20~40분, 중단해도 이어서
# .env 에 USE_EMBEDDING=true 확인
docker compose up -d --build
docker compose --profile tools run --rm eval     # 42문항 통과율
```

---

## 0-AB. CLOVA Studio 실연동 확인 완료 (2026-08-18)

AWS EC2 + Docker로 실배포한 뒤, 실제 코퍼스(158문서·8195청크)와 실제
`nv-` 키로 L1(질의분석)·L5'(답변생성)·L6(감독심사) 세 LLM 호출 지점이
전부 실제 HyperCLOVA X 호출로 성공하는 것을 확인했다. REVISE 재생성
경로(L6가 반려 → L5' 재생성 → 재검증)도 실제로 동작 확인됨.

가는 길에 3개 결함을 더 발견·수정(모두 이 브랜치에 push됨):

1. **키 형식 2종 미지원** — `nv-` Bearer 방식만 구현돼 있어 구형 콘솔 키가
   401. 접두사 자동판별 로직 추가(`app/llm/clova.py`).
2. **타임아웃이 실측 지연보다 타이트** — `CLOVA_TIMEOUT_SEC=8`이던 기본값이,
   실제 근거 문서가 실린 프롬프트(입력 3000토큰 안팎)의 5~8초 지연과
   거의 같아 정상 호출도 자주 타임아웃→재시도로 튕겨나갔다. 15초로 상향,
   `TOTAL_BUDGET_SEC`도 25→45초로 함께 조정.
3. **`.env`는 git으로 안 바뀐다** — 코드 기본값을 고쳐도 서버에 이미 있는
   `.env`의 값이 우선 적용되므로, 배포 환경에서는 `.env` 값도 함께
   수동으로 맞춰야 한다는 걸 다시 확인함(운영 문서에 반영).

⚠️ 관찰된 것: 정상 경로도 답변 생성 후 L6가 REVISE 판정을 내려 재생성이
도는 경우가 있었다(총 처리시간 25~35초대). 급한 문제는 아니지만, 왜
1차 생성이 자주 REVISE로 걸리는지는 시간 남으면 들여다볼 것.

---

## 0-AA. 실배포 중 발견 — CLOVA 키 형식 2종 미지원 (2026-08-18)

실물 코퍼스(158문서)로 재색인 후 첫 실배포 질의에서 L1·L5'·L6 세 LLM 호출이
전부 `HTTP 401 "Invalid Key - Please use new API Key that starts with 'nv-*'"`로
실패. 원인: `app/llm/clova.py`의 `_headers()`가 신형 `nv-` Bearer 인증만
구현하고 있었는데, 실제 발급받은 키는 `nv-`가 아닌 구형 콘솔 키 형식이었음.

CLOVA Studio 키는 발급 시기에 따라 두 형식이 있고 서로 호환되지 않는다:

| 형식 | 인증 방식 | 엔드포인트 |
|---|---|---|
| `nv-` 접두 (신형) | `Authorization: Bearer` | v3 |
| 그 외 (구형) | `X-NCP-CLOVASTUDIO-API-KEY` 헤더 (+선택 `X-NCP-APIGW-API-KEY`) | v1 (해당 앱의 실제 URL) |

**수정**: `ClovaClient`가 키 접두사(`nv-` 여부)를 보고 인증 헤더를 자동
판별하도록 함. 구형 키인데 엔드포인트가 여전히 `/v3/`이면(이 조합은 헤더를
아무리 맞춰도 401이 남) **클라이언트 생성 시점에 즉시 에러**로 막아 401을
받고 나서야 원인을 찾는 일을 방지. `.env.example`·`README.md`·
`docs/DEPLOY.md`에 두 형식의 설정 예시를 모두 추가하고,
`CLOVA_APIGW_KEY` 환경변수(구형 키에 동봉되는 API Gateway Key용, 선택)를
새로 뒀다. 회귀 테스트 6건 추가(`tests/test_llm_client.py`).

⚠️ **아직 실제 CLOVA Studio 서버로 검증 전** — 구형 헤더 스킴은 개발 컨테이너의
네트워크 차단으로 실호출 테스트를 못 했다. 배포 서버에서 `.env`의
`CLOVA_ENDPOINT`를 콘솔에 표시된 해당 앱의 실제 URL로 맞춘 뒤
`python -m app.llm.smoke_test`로 반드시 재확인할 것.

---

## 0-Z. 설계 대조 감사 (3차) — 결함 7건 발견·수정

"설계대로 안 된 부분"과 "HyperCLOVA X가 감독을 제대로 할 수 있는가"를 대조한 결과.

| # | 설계 원칙 | 구현 상태 | 심각도 |
|---|---|---|---|
| **1** | "결정론적 계층이 1차 방어선, LLM 감사는 보완" | **정반대였다.** LLM 호출 예산이 없으면 `supervise_hybrid`를 통째로 건너뛰어 준법·이상치·적합성·부담 4대 감사까지 사라졌다. 감독이 가장 필요한 순간(예산 부족·LLM 장애)에 감독이 0이 됨 | 🔴 |
| **2** | "계산은 함수, 설명은 LLM" | **조건 파싱이 서로 다른 항목의 금액을 합쳤다.** "총급여 4000만원인데 연금저축에 600만원" → 연금저축 4,600만원. 소득 5,000+납입 600이면 5,600만원이 되어 세액공제율이 16.5%→13.2%로 뒤집힘. **자신 있게 틀린 숫자**가 나오는 경로 | 🔴 |
| **3** | "감사는 심각도를 올릴 수만" | 이상치 감사가 `근속연수공제=5500`을 키에 '근속'이 있다는 이유로 연수(0~60)로 오분류해 **BLOCK**. doc52 원문 예시와 일치하는 정답이 차단되어 템플릿으로 축퇴됨 | 🔴 |
| **4** | "함정 D1 — 총보수 최저가 가입 불가일 수 있다" | 적합성 감사에 검색 후보를 그대로 넘겨 `eligible` 키가 없었다. **가입 불가 상품 감사가 한 번도 발동하지 않음.** 반대로 답변이 상품을 언급하지도 않았는데 등급이 강등되는 오탐도 있었음 | 🟠 |
| **5** | 부담 감사 4항목 | `partial_answer_possible`이 전달되지 않아 "부분 답변 가능한데 되묻기만 함"(AVOIDABLE_ASKBACK)이 **영구 비활성** | 🟠 |
| **6** | "L6는 의미 오류를 잡는다" | **감사자에게 도메인 함정을 알려주지 않았다.** 연차 2종 혼동 같은 걸 잡으라면서 무엇을 조심할지는 안 줘서, 감사자가 모델 내부 지식에 의존하게 됨 — 법 개정이 잦아 그걸 못 믿는다는 게 이 설계의 출발점인데 | 🟠 |
| **7** | "REVISE 시 1회 재생성" | 재생성이 성공해도 **옛 판정을 들고 있어** 아래 "수치검증 실패 → 템플릿 축퇴" 분기에 걸려 **성공한 재생성 결과를 버렸다** | 🟠 |

전부 수정 후 회귀 테스트로 고정했습니다 (`tests/test_design_conformance.py`, 29건).

### 설계대로 지켜지고 있던 것 (같은 감사에서 확인)

- **L0 산출물 격리** — `retrieved_context`·인용·계산 인자 어디에도 새지 않음.
  L1에는 `as_analysis_hint()`의 영역·용어 목록만 전달됨
- **권한 계층 단조성** — 결정론 REVISE + LLM APPROVE → REVISE 유지 (4×4 전조합)
- **감사 독립성** — 감사자에게 생성 프롬프트·추론 과정이 노출되지 않음
- **Exploration → Exploitation 순차** — 자격 검증 전에 수치 비교가 일어나지 않음
- **환각 차단** — LLM이 근거에 없는 수치를 넣으면 제거하고 결정론적 답변으로 축퇴

### LLM 호출 횟수 (크레딧 영향)

지시를 따르는 LLM 기준 **평균 2.67회/문항**(설계 가정 3개소 이내), 재생성 0/18.
단, 실연동 후 HyperCLOVA X가 교정 표현을 쓰지 않으면 재생성이 늘어 최대 5회가 됩니다.
그래서 L5' 프롬프트에 "교정 취지가 드러나는 표현을 쓰라"고 명시해 뒀습니다.
`GET /health`의 `llm_usage`로 실제 소모를 추적하십시오.

---

## 0-A. 재점검 결과 (2차) — 결함 3건 발견·수정

적대적 입력 15종과 예외 경로를 직접 찔러본 결과입니다.

| # | 결함 | 증상 | 상태 |
|---|---|---|---|
| **1** | **실물 문서를 넣어도 mock 코퍼스를 쓰던 문제** | `data/corpus/`에 PDF를 넣고 빌드했더니 "mock으로 만듭니다" 한 줄만 찍고 **가짜 문서로 인덱스 생성**. 그대로 평가받으면 제가 지어낸 문서를 근거로 답변 | ✅ 수정 |
| **2** | **구법 문서가 폴백 경로로 되살아남** (함정 C5 재발) | 근거가 전부 걸러지면 원본으로 되돌리는데, 이때 방금 제외한 700만원/1200만원 구법 문서가 다시 근거로 들어감 | ✅ 수정 |
| **3** | **계산함수 입력 범위 미검증** | "연금수령 0년차"(존재하지 않는 연차) → 한도 1,090만원, 나이 −5세 → 기타소득세 16.5%를 그럴듯하게 계산 | ✅ 수정 |
| 부수 | 깨진 네이티브 확장이 인제스트 전체를 죽임 | `pypdf`(pyo3)가 `BaseException`을 던져 `except Exception`을 뚫음 | ✅ 격리 |

**정상 확인된 것** (같은 점검에서 통과):
- 적대적 입력 15종(초장문·이모지·SQL·널바이트·음수·천문학적 숫자) 전부 5필드 200 유지
- LLM이 환각 수치(7,777만원)를 뱉어도 차단하고 정답(1,200만원) 유지
- LLM이 금지표현("추천드립니다")을 뱉어도 제거
- LLM이 JSON 대신 쓰레기를 뱉어도, 예외를 던져도 파이프라인 유지 + trace에 기록
- 인덱스가 비어 있으면 크래시 없이 거절

---

## 0. 아침에 확인할 것 (우선순위 순)

| # | 할 일 | 이유 | 소요 |
|---|---|---|---|
| **1** | **`.env`에 `CLOVA_API_KEY` 넣고 `python -m app.llm.smoke_test` 실행** | 지금 LLM이 **전혀 안 붙어 있다**. 응답 형식·인증 헤더·지연시간을 이걸로 먼저 확인해야 한다 | 10분 |
| **2** | 스모크 테스트가 출력하는 지연 기준으로 `app/pipeline.py`의 `BUDGET_*` 상수 갱신 | 현재 값은 실측 없이 잡은 추정치 | 5분 |
| **3** | 실제 제공 문서 zip을 `data/corpus/`에 넣고 `python -m app.ingest.inspect_zip data/corpus` 실행 | 지금은 **내가 만든 mock 문서**로 검색 중. 실물 zip 구조가 파서 가정과 맞는지 확인 필요 | 15분 |
| **4** | 디스코드에 **임베딩/NLI 모델 허용 여부** 질의 | 현재 BM25 단독. 허용되면 검색 품질이 크게 올라간다 | — |
| 5 | 미해결 2건(종신형+80세 세율 우선순위 / 이연퇴직소득 세율 구조) 팀 결정 | 코드는 손대지 않고 TODO만 남겼다 | — |
| 6 | `python -m tests.eval_set` 결과를 읽고 평가셋 문항 보강 | 지금 18문항은 내가 만든 것이라 편향이 있을 수 있다 | 30분 |

---

## 0-B. "API 키 + 파일만 넣으면 바로 되나?"

**거의 그렇지만, 세 가지가 남습니다.**

### ① 키를 넣는 것과 연동되는 것은 다릅니다
실호출을 **한 번도 성공시키지 못했습니다.** 인증 헤더 형식(Bearer vs
`X-NCP-CLOVASTUDIO-API-KEY`), 엔드포인트 모델 경로, Function calling 응답 스키마가
실제와 다를 수 있습니다. `python -m app.llm.smoke_test`가 이 셋을 한 번에 확인합니다.

**연동이 안 돼도 서비스는 뜹니다** — 규칙 기반 분석 + 결정론적 템플릿으로 답합니다.
다만 문장이 딱딱하고 "의도 충족"·"추론 논리성" 지표에서 손해를 봅니다.

### ② 파일은 형식에 따라 갈립니다

```bash
mkdir -p data/corpus && cp <제공 파일들> data/corpus/
python -m app.ingest.check_corpus     # ★ 반드시 먼저 — 몇 글자 읽혔는지 보여줍니다
python -m app.ingest.build_index
```

| 형식 | 결과 |
|---|---|
| zip(JPEG+OCR), txt, md, csv, tsv, json, html | 그대로 읽힘 |
| xlsx | 읽힘 (`openpyxl`) |
| PDF (텍스트 레이어 있음) | 읽힘 (`pypdf`) |
| **PDF (스캔본)** | **0자 — OCR 텍스트가 별도로 필요** |
| hwp, docx, pptx | 변환 후 넣어야 함 |

판독 실패는 조용히 넘어가지 않고 빌드 로그와 `GET /health`의
`corpus.skipped_files`에 남습니다.

### ③ DB는 없습니다 — 인덱스를 빌드해야 합니다
"DB에 파일을 넣는다"가 아니라 **파일을 넣고 `build_index`를 돌리는** 구조입니다
(외부 DB 불필요). `data/index/`는 `.gitignore`에 있어서 **저장소에 올라가지 않습니다.**
서버에서 반드시 한 번 빌드해야 합니다. Docker는 이미지 빌드 시 자동 실행됩니다.

### ④ ⚠️ GitHub에 자료를 올리는 건 권하지 않습니다

**이 저장소는 현재 public입니다** (`github.com/eunha9348/Pension-Agent`).

- 주최측 제공 자료(투자설명서·약관 등)를 공개 저장소에 올리는 것은
  재배포에 해당할 수 있습니다. 대회 규정과 자료 이용 조건을 먼저 확인하십시오.
- 용량 문제도 있습니다. 스캔 이미지가 포함된 전체 코퍼스(약 8,000건 규모)는
  GitHub 파일당 100MB 제한과 저장소 권장 용량을 넘길 가능성이 큽니다.

**권장**: 코퍼스는 git에 넣지 말고 배포 서버의 `data/corpus/`에 직접 복사하거나
볼륨으로 마운트하십시오. `.gitignore`에 `data/corpus/`를 추가해 두는 것이 안전합니다.
(저장소를 private으로 바꾸는 것도 방법이지만, 재배포 조건은 별개로 확인이 필요합니다.)

---

## 1. 완료한 Phase와 커밋

| Phase | 커밋 | 내용 |
|---|---|---|
| 0 | `ff54c43` | 프로젝트 구조, 7개 모듈 `app/core/` 이동, requirements/Dockerfile/.env.example/README |
| 1 | `a217aef` | CLOVA 클라이언트 + mock 폴백 + 스모크 테스트 |
| 3-1·2·2c | `845311f` | **calc_params_builder · slot_evidence_matcher · answer_covers_slot** (최우선) |
| 2 | `a06b938` | zip(JPEG+OCR) 인제스트, 청킹, 메타데이터, BM25 색인 |
| 3-4·5·6 | `1d3e134` | extract_query_spec · generate_answer · verify_grounding 래퍼 |
| 4 | `4ec3afb` | REFUSE 판정 경로 |
| 5·6 | `f133d57` | 6계층 파이프라인 통합 + `GET /answer` · `/health` |
| 7 | `57dab7a` | 회귀 테스트 스위트 + 자체 평가셋 18문항 |

Phase 8(배포)은 **하지 않았다** — 아래 3절 참고.

### 지금 바로 돌려볼 수 있는 것

```bash
python -m app.ingest.build_index          # 인덱스 생성 (mock 코퍼스)
python -m pytest -q                       # 190건
python -m tests.eval_set                  # 평가셋 리포트 18/18
uvicorn app.main:app --port 8000
curl "http://localhost:8000/answer?question_id=Q-001&question=계좌에 1억원 있고 연금수령 1년차인데 얼마까지 인출할 수 있나요?"
```

---

## 2. 막힌 것과 이유

### 2-1. CLOVA Studio 실연동 ❌ (가장 중요)

```
clovastudio.stream.ntruss.com:443 → CONNECT 403 (네트워크 정책 차단)
```

개발 컨테이너의 아웃바운드 정책상 해당 도메인에 나갈 수 없어서 **실제 호출을
한 번도 성공시키지 못했다.** 요청/응답 형식은 공개 규격에 맞춰 작성했지만
**검증되지 않았다.**

**API 키를 넣을 자리 — `.env` 파일 한 곳뿐이다:**

```bash
cp .env.example .env
# .env 를 열어서:
CLOVA_API_KEY=<여기에 발급받은 키>          # ← ★ 여기 ★
CLOVA_ENDPOINT=https://clovastudio.stream.ntruss.com/testapp/v1/chat-completions/HCX-005
LLM_MODE=auto        # auto면 키가 있을 때 자동으로 실호출로 전환
```

키를 넣은 뒤 **가장 먼저** 이걸 돌릴 것:

```bash
python -m app.llm.smoke_test
```

이 스크립트는 ① 인증이 되는지 ② 응답 JSON 구조가 파서와 맞는지
③ 지연시간이 얼마인지 ④ Function calling 응답 형태가 어떤지를 출력하고,
측정된 지연을 근거로 단계별 타임아웃 예산을 제안한다.

실패하면 확인 순서:
- **401/403** → 인증 헤더 형식. `app/llm/clova.py`의 `_headers()`가 Bearer 방식인데,
  구형 콘솔 키는 `X-NCP-CLOVASTUDIO-API-KEY` 헤더일 수 있다
- **404** → `CLOVA_ENDPOINT`의 모델 경로
- **toolCalls 파싱 실패** → `call_with_functions()`의 파싱부를 실제 스키마에 맞출 것

**지금 mock으로 돌고 있다는 표시는 세 군데에 있다:**
1. 서버 기동 로그에 `★` 배너
2. `GET /health` 의 `llm.is_mock: true`
3. 모든 응답의 `think_trace` 첫 줄 `[MOCK LLM] ...`

**mock은 내용을 지어내지 않는다.** 지어내면 실연동 시 문제가 안 드러나기 때문에,
각 호출 지점에서 "분석 실패"를 반환하고 결정론적 경로로 넘긴다:
- L1 → 규칙 기반 질의 분석기가 대신 처리
- L5' → 결정론적 템플릿 답변 (계산 결과 + 근거 문장만 조합, 환각 불가능)
- L6 → APPROVE + "감사 미수행" finding (권한 계층상 APPROVE는 판정을 완화하지 못해 안전)

### 2-2. 실제 제공 문서 ❌

문서를 아직 안 올리셨다고 하셔서, 말씀하신 구조(페이지별 JPEG + OCR 텍스트 zip)를
흉내낸 **mock zip 7건**을 만들어 파서를 검증했다 (`data/corpus_mock/`).

- PDF 라이브러리를 쓰지 않는다. `zipfile`로 구조를 먼저 확인한다
- 이미지 엔트리는 **읽지 않고** 목록만 센다 (OCR을 다시 돌리지 않는다)
- 실물 zip 구조를 모르므로 **4가지 레이아웃을 모두 흡수**하게 만들었다
  (A: 디렉터리 분리 / B: 평면 / C: JSON 통합 / D: 단일 텍스트)

실물을 받으면:
```bash
python -m app.ingest.inspect_zip data/corpus/<파일>.zip   # 구조 덤프 먼저
mv <실물 zip들> data/corpus/
python -m app.ingest.build_index                          # 실물이 자동 우선
```
`data/corpus/`에 zip이 하나라도 있으면 mock은 무시된다.

### 2-3. PostgreSQL / pgvector ⚠️ 정의만

컨테이너에 PG 서버가 없어 실행 검증을 못 했다. `sql/schema.sql`에 스키마만
정의해 두고, 기본 경로는 **파일 기반 인덱스 + 순수 파이썬 BM25**로 만들었다
(외부 DB 없이 동작). 검색 인터페이스가 같아서 PG로 바꿀 때 `app/ingest/store.py`의
백엔드만 교체하면 된다.

### 2-4. Phase 8 배포 ❌

NCP 콘솔 접근·도메인·인증서가 필요해 이 환경에서 할 수 있는 게 없다.
Dockerfile은 만들어 뒀다 (`docker build -t pension-agent .`).

---

## 3. 내가 내린 가정과 결정 (전부)

### 3-1. 지시하신 두 가지에 대한 처리

| 상황 | 결정 |
|---|---|
| CLOVA 차단 | mock 클라이언트로 인터페이스만 맞추고 나머지 Phase 전부 진행. mock은 **내용을 지어내지 않고** 결정론적 경로로 넘김 |
| 임베딩 허용 여부 불명 | **BM25 단독**(가장 보수적). 임베딩 의존 코드는 `app/retrieval/embedding.py` **한 파일에만** 격리. 허용되면 `embed_texts()` 하나 구현 + `.env`에서 `USE_EMBEDDING=true` |

### 3-2. CLAUDE.md와 다른 점 (확인 필요)

**CLAUDE.md에는 zip 구조(페이지별 JPEG + OCR) 설명이 없다.** 메시지로 주신
설명만 근거로 파서를 만들었다. CLAUDE.md에 그 문단을 추가해 두시는 게 좋겠다.

### 3-3. 설계 결정

1. **`calc_params_builder`의 3원칙**
   - 모르면 지어내지 않는다 → 슬롯 MISSING 강등 + ASK_BACK (확인 항목 최대 2건)
   - 경우의 수가 적으면 나눠서 전부 계산한다 → `__variants__`
     (소득 구간 미상이면 13.2%/16.5% 양쪽 다 계산 → "조건별 결론"의 재료)
   - 가정을 쓰면 반드시 기록한다 → `assumptions` → 답변 [한계 고지]

2. **`퇴직소득세_감면율_계산`에 연금수령연차를 절대 넣지 않는다** (함정 B1).
   "11년차"라고만 한 질의는 계산하지 않고 연금실제수령연차를 되묻는다.

3. **`과세방식_판정_계산`(DEPRECATED)은 호출 경로에서 자동으로
   `과세방식_비교_계산`으로 교정**한다. L1이 잘못 골라도 실행되지 않는다.

4. **근거 각주는 LLM이 아니라 `citation_system`이 붙인다.** 문서 ID를 LLM에게
   만들게 하면 없는 문서를 인용한다.

5. **타임아웃을 `run_with_timeout`(스레드)로 하지 않았다.** 파이썬은 스레드를
   죽일 수 없어 타임아웃 후에도 계속 돈다. 대신 `Deadline` 예산을 추적해
   남은 시간이 없으면 **해당 LLM 단계를 건너뛰고** 결정론적 경로로 축퇴시킨다.
   (계획서의 `asyncio.wait_for`도 같은 한계가 있다 — executor 스레드는 안 죽는다)

6. **세제 질의에서 구법 문서는 근거에서 제외**한다(환산으로 해소되지 않는 오답).
   세제 외 질의에서는 플래그만 달고 유지한다.

7. **근거가 0건이면 REFUSE.** 되묻기(조건 부족)와 거절(답할 수 없음)을 구분했다.

8. 계산 결과에 **`source` 필드가 없는 함수**(`calc_private_contribution_limit` 등)는
   손대지 않고, 파이프라인에서 계산 슬롯을 주제 매칭으로 근거 문서와 연결했다.
   (검증 완료 모듈을 수정하지 않기 위한 우회)

### 3-4. 기존 모듈 수정 내역 (최소한으로)

`app/core/coverage_pipeline.py` 두 곳만 고쳤다. 나머지 6개 모듈은 **무수정**이다.

- `run_calculations()` — 인자 조립을 `try` 안으로 옮기고(조건 부족과 계산 오류를
  구분해 trace에 기록), `__variants__` 조건 분기 계산 지원
- `decide_answerability()` — `refusal` / `evidence_count` 인자 추가로 REFUSE 경로 신설.
  기존 ANSWER/PARTIAL/ASK_BACK 동작은 회귀 테스트로 고정했다

### 3-5. 손대지 않은 미해결 항목 (TODO만)

지시대로 **건드리지 않았다**:
- 종신형 + 80세 이상 세율 우선순위 (규격서 순서상 4.4%) — 현재 동작만 테스트로 고정
- 이연퇴직소득 세율 서술 구조 (70/60/50 감면 vs 3.3% 고정)
- BM25 파라미터·점수 임계값 튜닝 근거 (`app/retrieval/bm25.py`)

---

## 4. 평가셋을 돌리다 발견해 고친 실제 결함 4건

1. **동의어 확장이 원 질의어와 같은 비중**이라, 동의어만 많은 짧은 청크(중도인출
   조항)가 정작 세액공제를 다루는 청크보다 상위로 올라왔다 → 확장 가중치 0.35 분리
2. **"한도"·"근속연수공제" 키워드 누락**으로 주제 인식 실패 → 배제어와 함께 보강
3. **납입액이 하나도 없는데 기본값 0으로 계산**해 "세액공제액 = 0만원"을 출력했다
   (맞지만 쓸모없고 오해를 부르는 답) → `REQUIRE_ANY` 가드로 확인 요청으로 전환
4. **근거 스니펫을 앞에서부터 잘라** 정작 필요한 세율표가 누락 → 슬롯 핵심어
   위치를 중심으로 발췌하고, 핵심어가 많이 나오는 청크를 우선 인용

추가로 표시 버그 1건: `적용 분모 = 10만원` (개수를 금액으로 표기) → 단위 명시 테이블 도입

---

## 5. 남은 작업 (2026-08-19 시점 — 전부 이후 완료됨)

- [x] CLOVA 실연동 검증 + 타임아웃 예산 재배분 — 08-18 확인 완료
- [x] 실제 문서 인제스트 및 검색 품질 재확인 — 158문서·8,172청크 인덱싱 완료
- [x] 임베딩 허용 확인 후 구현 — 2026-08-19 허용 확인, 로컬(e5-small) 연동
- [x] 실연동 후 L5'/L6 프롬프트 튜닝 — 이후 여러 차례 실측 반영
- [x] Phase 8 배포 — AWS EC2 + Docker Compose로 배포, Elastic IP 할당 완료
- [x] Sub-Agent(전 구간 건전성 감독)·법령 계층·상품 팩트 6축 신설 (당시엔 없던 항목)

**현재 남은 작업은 이 파일이 아니라 CLAUDE.md와 대화 세션의 작업
추적기를 참고하십시오.** 09.06 마감 전 최종 점검(T6)이 진행 중입니다.

---

## 6. 구조 요약

```
app/
  config.py              환경 설정 (.env 로더)
  core/                  ← 기존 검증 완료 7개 모듈 (거의 무수정)
  llm/clova.py           CLOVA 클라이언트 + MockClovaClient
  llm/smoke_test.py      ★ 키 넣고 가장 먼저 돌릴 것
  ingest/                zip 파서 · 청킹 · 메타데이터 · 인덱스 빌드
  retrieval/             BM25 · L0 개략검색 · L3 하이브리드 · ★임베딩 격리★
  analysis/              L1 질의분석 · 조건 정규화 · 계산 인자 · 슬롯 매칭 · 거절 판정
  generation/            L5' 프롬프트 · 결정론적 템플릿 · 근거 검증 래퍼
  pipeline.py            L0~L6 통합
  main.py                GET /answer · /health
tests/                   190건 + eval_set 18문항
data/corpus_mock/        ⚠️ 내가 만든 mock 문서 (실물 아님)
sql/schema.sql           PostgreSQL 스키마 (정의만, 미검증)
```
